# Decision Core invariants

## Scope through Phase 8D6

The Phase 8D4A section below extends the existing cumulative enforcement record through Phase 8D4A; all earlier invariant material remains in force.

## Phase 8D6 observation item materialization invariants

- Materialization accepts exactly one complete sealed readiness predecessor and no independent item, Context, or revision input.
- It constructs exactly one standalone four-field item from retained readiness lineage: `itemId` equals `candidateItemId`, role remains `OBSERVATION`, and statement and provenance match projection exactly.
- Statement provenance is preserved: HUMAN_INPUT remains HUMAN_INPUT, MODEL_PROPOSAL remains MODEL_PROPOSAL, and AUTHORITATIVE_STATE remains AUTHORITATIVE_STATE; it is not rewritten to `DETERMINISTIC_DERIVATION`.
- The five-field artifact retains complete readiness plus item. DCOIM identity commits to schema, canonical complete readiness, and canonical complete item, with object insertion order non-semantic.
- Item existence remains isolated: ITEM EXISTENCE != CONTEXT MEMBERSHIP. MATERIALIZATION != ITEM MEMBERSHIP. MATERIALIZATION != CONTEXT MEMBERSHIP. MATERIALIZATION != CONTEXT MUTATION. MATERIALIZATION != REVISION MUTATION. MATERIALIZATION != REVISION CREATION. MATERIALIZATION != REVISION TRANSITION. MATERIALIZATION != PERSISTENCE. MATERIALIZATION != PERSISTENCE AUTHORITY. MATERIALIZATION != LOOP CLOSED.
- Construction and stored assertion are descriptor-safe, detached, reader-free, resolver-free, Context-construction-free, revision-construction-free, and persistence-free. Accessors, hidden fields, symbol keys, extras, hostile nested readiness, item, and provenance state reject without getter execution; stored body invalidity precedes stale DCOIM classification and assertion repairs nothing.
- Materialization does not promote epistemic or authority status: MATERIALIZATION != OBSERVATION TRUTH. MATERIALIZATION != OBSERVED REALITY. MATERIALIZATION != OUTCOME TRUTH. MATERIALIZATION != SEMANTIC SUPPORT. MATERIALIZATION != CAUSATION. MATERIALIZATION != HUMAN DECISION. MATERIALIZED OBSERVATION != TRUE OBSERVATION. AUTHORITATIVE_STATE PROVENANCE != SOURCE TRUTH. SOURCE INVENTORY MEMBERSHIP != EXTERNAL AUTHORITY. MODEL_PROPOSAL PROVENANCE != MODEL FACT. HUMAN_INPUT PROVENANCE != VERIFIED FACT. PERSISTED != TRUE.

## Phase 8D5 observation materialization readiness invariants

- Readiness has exactly one complete sealed target-revision binding predecessor; no independent role, statement, provenance, or target input exists.
- The predecessor is descriptor-safely captured and sealed-asserted before Phase 8D5 derives any semantic value. Accessors reject without getter execution; hidden, symbol, and extra properties reject where applicable.
- The retained revision is base state. BOUND REVISION != MUTATION DESTINATION. READINESS != CONTEXT MUTATION. READINESS != REVISION MUTATION. READINESS != REVISION CREATION. READINESS != REVISION TRANSITION.
- Candidate identity is the existing deterministic `DCI_` identity for the retained projected `OBSERVATION` role, statement, and provenance. CANDIDATE ITEM ID != DECISION CONTEXT ITEM. CANDIDATE ITEM ID != MATERIALIZATION. CANDIDATE ITEM ID != CONTEXT MEMBERSHIP.
- `HUMAN_INPUT` and `MODEL_PROPOSAL` introduce no source inventory condition. `AUTHORITATIVE_STATE` requires exact structural membership of the projected reference in the bound base Context inventory; absence is `ERR_DECISION_CONTEXT_OBSERVATION_MATERIALIZATION_READINESS_SOURCE_REFERENCE_MISSING`.
- Inventory membership is not an authority operation: SOURCE REFERENCE PRESENT != SOURCE AUTHORITY RESOLVED. SOURCE REFERENCE PRESENT != SOURCE AUTHENTICATED. SOURCE REFERENCE PRESENT != SOURCE CURRENT. SOURCE REFERENCE PRESENT != SOURCE TRUE. SOURCE REFERENCE PRESENT != SEMANTIC SUPPORT. SOURCE REFERENCE PRESENT != CAUSATION. SOURCE STATE INVENTORY MEMBERSHIP != EXTERNAL AUTHORITY.
- The candidate ID must be absent from the bound base Context items; presence is `ERR_DECISION_CONTEXT_OBSERVATION_MATERIALIZATION_READINESS_ITEM_ALREADY_PRESENT`. ITEM ALREADY PRESENT != RETURN PATH MATERIALIZED. ITEM ALREADY PRESENT != LOOP CLOSED. ITEM ID ABSENT != SEMANTIC NOVELTY. ITEM ID ABSENT != TRUTH. ITEM ID ABSENT != PRIORITY.
- The readiness artifact has exactly `artifactKind`, `schemaVersion`, `decisionContextObservationMaterializationReadinessId`, `decisionContextObservationTargetRevisionBinding`, and `candidateItemId`. It is positive readiness only: no status, score, confidence, ranking, priority, recommendation, threshold, or human-approval field exists.
- `DCOMR_` identity commits to schema, canonical complete binding, and candidate ID using SHA-256 first 24 uppercase hex. Object insertion order is non-semantic. The complete binding representation is identity-bearing; readiness identity is not truth proof.
- Stored assertion is self-contained and reader-free, resolver-free, construction-free, and persistence-free. It rechecks complete binding, candidate identity, applicable inventory membership, candidate absence, and DCOMR identity; body invalidity precedes DCOMR mismatch; it repairs nothing.
- READINESS != MATERIALIZATION. READINESS != MATERIALIZATION AUTHORITY. READINESS != DECISION CONTEXT ITEM. READINESS != ITEM MEMBERSHIP. READINESS != CONTEXT MEMBERSHIP. READINESS != PERSISTENCE. READINESS != PERSISTENCE AUTHORITY. READINESS != OBSERVATION TRUTH. READINESS != OBSERVED REALITY. READINESS != OUTCOME TRUTH. READINESS != SEMANTIC SUPPORT. READINESS != CAUSATION. READINESS != HUMAN DECISION. READINESS != LOOP CLOSED. PERSISTED != TRUE.

## Phase 8D4B exact revision-binding invariants

- A binding is created only by the explicit bound reader operation; a target declaration does not automatically create one.
- Before one reader call, the binder descriptor-safely captures and sealed-asserts the complete declaration, then captures its `targetRevisionId` before the await boundary.
- Exactly one `getRevisionById(targetRevisionId)` occurs. `null` is `REVISION_NOT_FOUND`; a malformed returned state or unequal returned `revisionId` is `REVISION_INVALID`; an operational reader rejection propagates unchanged.
- The five-field binding embeds complete detached declaration and complete detached revision state. Its stored assertion performs no read, repairs nothing, rejects hostile accessors without executing getters, rejects symbols/hidden/extra properties where applicable, and gives malformed body invalidity precedence over binding-ID mismatch.
- `DCOTRB_` commits to canonical complete declaration and revision representation. `COMPLETE REVISION STATE != DREV STRING ALONE`.
- `REVISION BINDING != PERSISTENCE AUTHORITY`; `REVISION BINDING != AUTHORITY OF RECORD`; `REVISION BINDING != CURRENT REVISION`; `REVISION BINDING != HEAD REVISION`; `REVISION BINDING != LATEST REVISION`; `REVISION BINDING != ACTIVE REVISION`; `REVISION BINDING != REVISION SELECTION`; `REVISION BINDING != MUTATION DESTINATION`; `REVISION BINDING != FUTURE REVISION`.
- `REVISION BINDING != MATERIALIZATION`; `REVISION BINDING != MATERIALIZATION READINESS`; `REVISION BINDING != DECISION CONTEXT ITEM`; `REVISION BINDING != ITEM MEMBERSHIP`; `REVISION BINDING != CONTEXT MEMBERSHIP`; `REVISION BINDING != CONTEXT MUTATION`; `REVISION BINDING != REVISION MUTATION`; `REVISION BINDING != REVISION CREATION`; `REVISION BINDING != REVISION TRANSITION`; `REVISION BINDING != LOOP CLOSED`.
- `REVISION BINDING != SOURCE STATE REFERENCE ADMISSION`; `REVISION BINDING != SOURCE STATE INVENTORY MEMBERSHIP`; `REVISION BINDING != OBSERVATION TRUTH`; `REVISION BINDING != OBSERVED REALITY`; `REVISION BINDING != OUTCOME TRUTH`; `REVISION BINDING != SEMANTIC SUPPORT`; `REVISION BINDING != CAUSATION`; `REVISION BINDING != HUMAN DECISION`; `PERSISTED != TRUE`.

This table records enforcement present through Phase 8D3. Phase 5C2 records semantic evaluator proposals, Phase 5C3A records explicit structural comparison targets, Phase 5C3B records explicit structural relation proposals, Phase 5C3C derives basis-relative structural gaps, Phase 5C3D derives explicit-path basis-relative `StructuralConsequence` artifacts from validated item-anchored gaps and explicit ordered dependency paths, Phase 5C4 assembles revalidated derivational coherence, Phase 5D1 constructs self-contained canonical revision artifacts, Phase 5D2A establishes repository-bound immutable authority-of-record operations, Phase 5D2B durably implements those sealed persistence semantics in PostgreSQL, Phase 5D3 reconstructs one explicit sealed-valid predecessor path read-only through a bound reader capability, Phase 6A records one standalone human-declared normative assessment request through shape-only DREV/DCI references, Phase 6B binds that request to one exact reader-returned sealed revision with membership/role verification, Phase 6C records admitted model semantic assessment proposal relations within that selected frame, Phase 6D records admitted recommendation proposal state for selected, assessment-represented options, Phase 6E deterministically materializes assessment-criterion traceability for each recommendation, Phase 7A records explicit positive human choices from the complete sealed revision `OPTION` ontology, Phase 8A1 records opaque intended operation state only for an explicit subset of that human choice set, Phase 8A2 records one declared human commitment to one complete Action Intent, Phase 8B records standalone source-attributed `ActionOccurrenceClaim` state, Phase 8C1 records standalone source-attributed `StateChangeClaim` state, Phase 8C2 records one explicit provenance-attributed `ActionStateChangeAssociationProposal` over those two sealed claim roles, Phase 8C3 records one explicit provenance-attributed `OutcomeAttributionProposal` over one sealed association proposal, Phase 8D1 records one explicit provenance-attributed opaque `DecisionContextObservationProposal` statement as an `OBSERVATION`-role candidate for a future Decision Context, Phase 8D2 records one positive declared human `DecisionContextObservationAdmissionDeclaration` over one sealed observation proposal as eligible for future `OBSERVATION`-role materialization, and Phase 8D3 records one deterministic `DecisionContextObservationItemProjection` of exact future `OBSERVATION`-item input semantics over one sealed admission declaration. Phase 8B establishes neither action fact, observation truth, execution proof, semantic occurrence support, nor authority of reality. Phase 8C1 establishes neither state-change fact, observation truth, verified change, effect, outcome, consequence, causal claim, semantic state-change support, nor authority of reality. Phase 8C2 establishes neither association truth, outcome, effect, consequence, attribution, causation, semantic support, current authority, authority of reality, nor persistence authority. Phase 8C3 establishes neither outcome truth, relation truth, causal claim, causation, semantic support, current authority, publication authority, authority of reality, nor persistence authority. Phase 8D1 establishes neither observation truth, outcome truth, semantic support, Decision Context admission, Context Item materialization, revision, loop closure, current authority, authority of reality, nor persistence authority. Phase 8D2 establishes neither materialization, Context mutation, revision creation, loop closure, observation truth, observed reality, outcome truth, semantic support, causation, authentication, external authorization, authority of reality, nor persistence authority. Phase 8D3 establishes neither item existence, Context membership, materialization, Context mutation, revision, loop closure, observation truth, observed reality, outcome truth, semantic support, causation, current authority, authority of reality, nor persistence authority. None of the implemented contracts establishes verified semantic truth, global completeness, current producer authority, current decision state, decision readiness, decision need, recommendation correctness, responsibility, ownership, accountability, assignment, authorization, action, execution, outcome, feedback, or learning.

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Authority dependencies are bound at construction. | Callers must not redirect an existing reader or validator with a replacement resolver method. | `authority/reader.ts`; `validation/authority.ts`. | Reader/validator capture and bind `resolve` once, preserving `this`. Contract tests mutate methods after construction. |
| `AuthoritativeStateReference` carries no payload or resolver. | State content and authority dependency must remain separate from caller input. | `authority/types.ts`; strict reader reference capture. | Type-level/runtime contract tests; malformed runtime references fail `ERR_DECISION_AUTHORITY_REFERENCE_INVALID`. |
| A successful resolution does not confer portable authority. | A returned object must not certify future authority. | `AuthoritativeStateResolution` documentation and reader/validator APIs. | Reader returns a descriptive resolution; 5C1 returns `Promise<void>`. No API accepts a prior resolution as authority. |
| Resolver identity is `producerId + authorityContractId`. | A producer contract, not caller payload shape, selects a resolver. | `resolverBindingKey`; bound-reader map. | Unknown binding: `ERR_DECISION_AUTHORITY_RESOLVER_NOT_FOUND`; duplicate/invalid registration: `ERR_DECISION_AUTHORITY_RESOLVER_CONFLICT`. |
| The Capability adapter validates a persisted Phase-4 snapshot before returning state. | A SNAP-like shape or prefix is not sufficient producer authority. | `decision-adapters/capability-core.ts`. | Missing state: `ERR_DECISION_AUTHORITY_STATE_NOT_FOUND`; invalid snapshot: `ERR_DECISION_AUTHORITY_STATE_INVALID`; locator/artifact mismatch: `ERR_DECISION_AUTHORITY_ARTIFACT_REFERENCE_MISMATCH`. |
| A context has exactly one `DECISION_QUESTION`. | `decisionQuestionId` is derived from one canonical question item. | `context/contract.ts`. | `ERR_DECISION_CONTEXT_DECISION_QUESTION_COUNT`. |
| Zero options, objectives, and constraints are valid. | Phase 5B represents structural drafts before completeness/gap analysis. | Context constructor/assertion contract. | No error solely for their absence. |
| Role and provenance are independent axes. | An objective/observation/constraint does not establish who supplied it or its epistemic status. | `DecisionContextItem` plus discriminated provenance union. | Identity includes both role and canonical provenance; no origin conversion occurs. |
| Provenance origins are closed in Phase 5B. | The draft records source category without collapsing categories. | `DecisionContextItemProvenance`. | Exactly `AUTHORITATIVE_STATE`, `HUMAN_INPUT`, `MODEL_PROPOSAL`, `DETERMINISTIC_DERIVATION`; invalid fields/origins fail `ERR_DECISION_CONTEXT_ITEM_INVALID`. |
| Draft validation status is always `NOT_RUN`. | Structural construction does not resolve or semantically validate authority. | `DecisionContextDraft`; constructor/assertion. | Any other status fails `ERR_DECISION_CONTEXT_INVALID`. |
| `DCI_` identity is deterministic. | Same canonical item content must have one item identity independent of input position. | `context/identity.ts`. | Item IDs are recomputed during assertion; mismatch fails `ERR_DECISION_CONTEXT_ITEM_ID_MISMATCH`. |
| `DCTX_` identity is deterministic. | Canonical reference/item sets must produce one canonical context artifact. | `context/identity.ts`; constructor/assertion. | Context mismatch: `ERR_DECISION_CONTEXT_ID_MISMATCH`. |
| Source references are canonical and unique. | A source inventory must not silently duplicate or reorder identity-bearing references. | Context constructor/assertion. | Duplicate: `ERR_DECISION_CONTEXT_DUPLICATE_SOURCE_STATE_REFERENCE`; noncanonical order: `ERR_DECISION_CONTEXT_SOURCE_STATE_REFERENCES_NOT_CANONICAL`. |
| Context items are canonical and identity-checked. | Stored item content cannot diverge from its ID or order. | Context constructor/assertion. | Duplicate: `ERR_DECISION_CONTEXT_DUPLICATE_ITEM`; noncanonical order: `ERR_DECISION_CONTEXT_ITEMS_NOT_CANONICAL`; invalid item: `ERR_DECISION_CONTEXT_ITEM_INVALID`. |
| `AUTHORITATIVE_STATE` provenance must refer to a listed source reference. | The draft may reference authority only through its declared source inventory. | `assertQuestionAndAuthoritativeReferences`. | `ERR_DECISION_CONTEXT_AUTHORITATIVE_REFERENCE_MISSING`. This is structural presence, not current resolvability. |
| Context carries no later-phase recommendation/decision semantics. | A draft is not assessment, recommendation, decision, action, outcome, or feedback state. | `DecisionContextDraft`/`DecisionContextItem` type contracts and public-export gates. | No such fields/types are exported by the Phase-5B context surface. |
| 5C1 validates the complete context before resolution. | A malformed context must not trigger any reader call. | `captureContextReferences` then `assertDecisionContextDraft`. | `ERR_DECISION_CONTEXT_AUTHORITY_CONTEXT_INVALID`; contract test proves zero reader calls. |
| 5C1 resolves detached captured references. | Caller mutation after validation begins must not change later resolver inputs. | `validation/authority.ts`. | Detached capture before awaits; tests cover asynchronous caller mutation. |
| Reader mutation cannot redirect an existing validator. | The reader is a construction-time authority dependency. | `bindResolve`. | Invalid reader: `ERR_DECISION_CONTEXT_AUTHORITY_READER_INVALID`; replacement method is not invoked. |
| Returned resolution reference must equal the requested reference exactly. | A resolver result for another artifact cannot satisfy the request. | `captureResolutionReference`; `sameReference`. | `ERR_DECISION_CONTEXT_AUTHORITY_REFERENCE_MISMATCH`. |
| 5C1 ignores resolution payload semantics. | Authority reachability is not semantic evidence binding. | Validator loop reads only `resolution.reference`. | Payload is neither returned nor inspected, hashed, persisted, or interpreted. |
| Empty authority inventory is valid. | A context may legitimately have no source references before later completeness analysis. | Validator loop. | Zero bound-reader calls; successful `Promise<void>`. |
| Successful 5C1 validation returns `Promise<void>`. | It must not create a reusable authority bundle, certificate, or validated context artifact. | `BoundDecisionContextAuthorityValidator`. | Success has no value; later operations must resolve again. |
| Validation production remains generic. | Generic Decision Core must not take on producer/domain ontology. | `test/decision-core/validation/authority.test.ts` AST import gate. | Validation sources may not import Career, Capability Core, matching, recommendation, or legacy decision-looper paths. |
| Validation public surface excludes Phase-5C2+ concepts. | Later semantic-analysis/decision artifacts must not appear early as public types or values. | Compiler-resolved validation export gate. | No public `EvidenceBinding`, support states, gaps, contradictions, dependencies, consequences, recommendations, scores, rankings, human decisions, actions, outcomes, or feedback. |
| Phase-5C2 evaluator and reader dependencies are bound at construction. | A caller must not redirect an existing semantic binder by replacing methods after composition. | `evidence-binding/binder.ts`. | `bindResolve` and `bindEvaluate` capture methods with receivers; contract tests prove replacement resistance and `this` preservation. |
| A binding is exactly one item × one listed state reference. | Semantic support is an explicit relation, not inferred from context role or provenance. | `SemanticEvidenceBindingProposal`; evaluator-output capture. | Unknown item: `ERR_DECISION_EVIDENCE_BINDING_ITEM_NOT_FOUND`; foreign/mismatched reference: `ERR_DECISION_EVIDENCE_BINDING_STATE_REFERENCE_INVALID`. |
| Binding dispositions are closed. | Phase 5C2 must not encode uncertainty, confidence, score, or ranking. | `EVIDENCE_BINDING_DISPOSITIONS`. | Only `SUPPORTED`, `PARTIALLY_SUPPORTED`, `NOT_SUPPORTED`, `CONTRADICTED`; malformed output fails `ERR_DECISION_EVIDENCE_BINDING_EVALUATION_INVALID`. |
| Provenance does not imply semantic support. | Origin and item/state semantic relationship are independent axes. | Binder accepts evaluator proposals for any provenance. | Tests show `HUMAN_INPUT` may be `SUPPORTED`, `MODEL_PROPOSAL` may be `CONTRADICTED`, and `AUTHORITATIVE_STATE` has no automatic support. |
| Complete operation-time authority preparation precedes semantic evaluation. | No evaluator may inspect one state while another context source remains unresolved or unsafe. | Stage A followed by Stage B in `evidence-binding/binder.ts`. | Any resolution, returned-reference, or payload-isolation failure invokes the evaluator zero times. |
| Semantic payload inspection cannot mutate resolver-owned producer payload. | Shared mutable producer memory is not semantic evaluator capability. | `detachSemanticPayload`; shared-memory graph inspection. | Non-cloneable or shared-memory payload: `ERR_DECISION_EVIDENCE_BINDING_PAYLOAD_NOT_DETACHABLE`; evaluator is not invoked. |
| Semantic payload isolation is Phase-5C2-specific. | Generic Phase-5A resolution returns a resolver-owned opaque payload without cloning it. | Stage-A `structuredClone` plus post-clone inspection. | Phase 5C2, not Phase 5A, establishes evaluator payload isolation. |
| `EBIND_` identity excludes rationale wording. | Free-language explanation must not alter the identity of the item/state/disposition relationship. | `evidence-binding/identity.ts`. | Exact identity tests prove rationale changes retain ID; disposition changes ID. |
| Duplicate item/reference binding targets reject and output is canonical. | The same relationship must not silently carry divergent evaluator proposals or order-dependent output. | Target-key and ID sets; deterministic binding-ID sort. | `ERR_DECISION_EVIDENCE_BINDING_DUPLICATE`; canonical `SemanticEvidenceBindingProposal[]`. |
| Zero semantic proposals are valid. | Unexamined relationships are not negative support findings, and Phase 5C2 does not assess completeness. | Evaluator/binder contract. | `[]` returns without synthesizing `NOT_SUPPORTED`; no gap/completeness claim. |
| `CONTRADICTED` is not a structural contradiction finding. | A single evaluator proposal cannot establish the later cross-binding/context Phase-5C3 concept. | Phase-5C2 public contract/export gate. | No public `Contradiction`, Gap, Dependency, Consequence, Recommendation, Score, Ranking, HumanDecision, Action, Outcome, or Feedback artifact. |
| Evidence-binding production remains generic and excludes Phase-5C3+ concepts. | The generic kernel must not acquire Career/Capability semantics or future decision-analysis surface. | `test/decision-core/evidence-binding/contract.test.ts` AST import/public-export gates. | No Career, Capability Core, matching, recommendation, or legacy decision-loop import; no forbidden Phase-5C3+ public export. |
| Absence is not a Gap. | Missing items, roles, bindings, or dispositions have no defined comparison target until an explicit expectation exists. | `structural-findings/expectation.ts` exposes construction/assertion only. | Phase 5C3A derives or represents no Gap and performs no satisfaction evaluation. |
| A `StructuralExpectation` is a comparison target only. | Expected structure must remain distinct from observed structure and from a derived finding. | `StructuralExpectation` contract. | Phase 5C3A performs structural membership checks for referenced items and authoritative-state provenance references, but performs no expectation-satisfaction evaluation against current role counts, `SemanticEvidenceBindingProposal[]`, dependency findings, or other observed relation state. |
| Structural expectation kinds are closed. | Phase 5C3A must not encode findings, decision need, priority, or recommendations as expectations. | `STRUCTURAL_EXPECTATION_KINDS`. | Exactly `EVIDENCE_BINDING`, `CONTEXT_ROLE`, and `DEPENDENCY`; no fourth kind. |
| Expectation provenance is origin only. | A human, model, deterministic, or authoritative-state origin must not establish fact, requirement, truth, or satisfaction. | Reused `DecisionContextItemProvenance`; expectation identity tuple. | No origin conversion occurs; model proposal remains proposal state and human input remains distinct. |
| `AUTHORITATIVE_STATE` expectation provenance requires context membership only. | The reference must be declared by the context without treating that membership as current authority or satisfaction. | `requireContextReference`. | Missing or malformed/unlisted reference: `ERR_DECISION_STRUCTURAL_EXPECTATION_REFERENCE_INVALID`; no authority resolution occurs. |
| Phase 5C3A performs no authority resolution or semantic binding evaluation. | Structural expectation construction must not consume payloads or conflate expectation with support. | `structural-findings/expectation.ts`. | No reader, resolver, repository, evaluator, or binder dependency exists in the API. |
| Evidence-binding accepted dispositions are explicit and canonical. | An expectation criterion must be deterministic without asserting that any binding exists. | `canonicalizeDispositions`. | Only sealed Phase-5C2 dispositions; stored order follows `SUPPORTED`, `PARTIALLY_SUPPORTED`, `NOT_SUPPORTED`, `CONTRADICTED`. |
| Duplicate accepted dispositions reject rather than deduplicate. | Caller ambiguity must not be silently repaired. | `canonicalizeDispositions`. | `ERR_DECISION_STRUCTURAL_EXPECTATION_DUPLICATE_DISPOSITION`. |
| `CONTEXT_ROLE.minimumCount` is a positive safe integer. | A structural threshold must be finite and meaningful without evaluating it. | `captureInput`. | Invalid role/count: `ERR_DECISION_STRUCTURAL_EXPECTATION_INPUT_INVALID`. |
| Dependency expectation direction is identity-bearing and self-dependency rejects. | A depends on B differs from B depends on A; an item cannot be its own prerequisite in this contract. | Dependency canonical body; `captureInput`. | Direction changes `DEXP_`; self-dependency: `ERR_DECISION_STRUCTURAL_EXPECTATION_INPUT_INVALID`. |
| `DEXP_` identity is deterministic. | One canonical context/kind/body/provenance tuple must produce one explicit expectation identity. | `structural-findings/identity.ts`. | Recomputed during assertion; mismatched valid stored ID: `ERR_DECISION_STRUCTURAL_EXPECTATION_ID_MISMATCH`. |
| Constructor canonicalization differs from stored-artifact assertion. | Canonical equivalence must not permit a mutable stored representation to be silently repaired. | `createStructuralExpectation`; `assertStoredDispositionsCanonical`. | Constructor orders valid input; a reordered stored `acceptedDispositions` representation fails `ERR_DECISION_STRUCTURAL_EXPECTATION_INVALID`. |
| Returned expectation artifacts detach from caller input. | Later mutation of nested caller-owned arrays or provenance objects must not alter the constructed artifact. | Recursive descriptor capture plus `structuredClone` return. | Contract test mutates caller-owned input after construction; artifact remains unchanged. |
| Structural-findings public surface excludes findings and decision concepts. | Explicit expectations must not accidentally expose Gap, Dependency finding, or decision-intelligence artifacts. | Compiler-resolved export gate in `expectation.test.ts`. | No public Gap, Contradiction, Dependency finding, Consequence, DecisionNeed, Recommendation, Score, Confidence, HumanDecision, Action, Outcome, or Feedback. |
| Structural-findings production remains generic. | The generic kernel must not acquire Career, Capability Core, matching, recommendation, or legacy decision-loop coupling. | AST module-specifier gate in `expectation.test.ts`. | No forbidden production import. |
| Structural relation proposals are proposals only. | Caller-supplied relation content must remain distinct from relation truth and findings. | `StructuralRelationProposal` contract. | No relation validation, finding, Gap, Consequence, Decision Need, or recommendation artifact is constructed. |
| Relation ontology does not perform relation discovery. | Item roles, text, provenance, semantic bindings, expectations, authority state, similarity, and absence are not relation detectors. | `structural-findings/relation.ts` accepts only context plus explicit relation input. | No detector, evaluator, analyzer, inference engine, or graph traversal API exists. |
| CONTRADICTION is symmetric but stored canonicality is strict. | Equivalent constructor endpoint order must not create alternate proposals, while stored representations cannot be silently repaired. | Contradiction input capture and stored canonicality assertion. | Constructor sorts distinct endpoints deterministically; reversed stored endpoints fail `ERR_DECISION_STRUCTURAL_RELATION_INVALID`. |
| DEPENDENCY is directional and identity-bearing. | A dependent/prerequisite proposal is not interchangeable with its reverse. | Dependency relation body and DREL calculation. | Reversing otherwise valid stored endpoints with the old ID fails `ERR_DECISION_STRUCTURAL_RELATION_ID_MISMATCH`; self relation fails `ERR_DECISION_STRUCTURAL_RELATION_INPUT_INVALID`. |
| Relation endpoints require context item membership. | A proposal can only name items from its bound context. | `requireContextItem`. | Missing endpoint: `ERR_DECISION_STRUCTURAL_RELATION_ITEM_NOT_FOUND`. |
| `AUTHORITATIVE_STATE` relation provenance requires context-reference membership only. | Origin membership must not become current authority proof or relation truth. | `requireContextReference`. | Malformed or unlisted reference: `ERR_DECISION_STRUCTURAL_RELATION_REFERENCE_INVALID`; no authority resolution occurs. |
| Relation provenance is identity-bearing but not relation truth. | Human, model, deterministic, and authoritative origins must stay distinct without establishing truth. | Canonical provenance in the DREL tuple. | Changing provenance changes `DREL_`; no origin conversion or truth claim occurs. |
| `DREL_` identity is deterministic. | A canonical context/kind/body/provenance tuple must have one local proposal identity. | `structural-findings/relation-identity.ts`. | Recomputed during assertion; wrong ID on an otherwise valid canonical proposal fails `ERR_DECISION_STRUCTURAL_RELATION_ID_MISMATCH`. |
| Stored representation validation precedes variant reparsing. | Missing or extra stored top-level fields are representation failures, not constructor-input failures. | `proposalInputFromArtifact`. | Hostile/malformed stored representation, invalid header, missing/extra keys, and noncanonical contradiction order fail `ERR_DECISION_STRUCTURAL_RELATION_INVALID`; exact stored representation with invalid variant content may preserve specific errors. |
| Returned relation artifacts detach from caller input. | Mutation of caller-owned endpoint arrays or provenance objects cannot alter a constructed proposal. | Recursive descriptor capture plus `structuredClone` return. | Contract test mutates nested constructor input; returned proposal remains unchanged. |
| Phase 5C3B performs no authority resolution or semantic evaluation. | Relation proposal representation must not consume producer payloads or semantic binding logic. | `structural-findings/relation.ts`. | No reader, resolver, repository, evaluator, binder, or expectation dependency exists in the API. |
| Structural-findings public surface excludes findings and decision concepts. | Relation proposals must not accidentally expose later structural or decision artifacts. | Compiler-resolved export gate in `relation.test.ts`. | No public Gap/finding/Consequence/Decision Need/priority/score/ranking/recommendation/human-decision/action/outcome/feedback concepts or relation identity helpers. |
| Structural relation production remains generic. | The proposal contract must not acquire Career, Capability Core, matching, recommendation, legacy loop, Phase-5C2, or Phase-5C3A coupling. | AST module-specifier gate in `relation.test.ts`. | No forbidden production import. |

## Phase 5C3C structural-gap invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| A gap requires an explicit expectation and an explicit represented basis. | Absence alone is not a Gap. | `structural-gaps/reconstruct.ts`. | One `StructuralExpectation` is compared per `reconstructStructuralGap(context, expectation, basis)` call. |
| A structural gap is basis-relative. | The represented basis is not a complete model of external reality. | Canonical gap body includes relevant observations from the supplied basis. | Different relevant non-satisfying bases can derive different `DGAP_` values; a satisfying basis returns `null`. |
| `null` is not global completeness. | One expectation can produce no gap without proving every expectation or the real world is complete. | Reconstruction return contract. | `null` means only this expectation is satisfied within this supplied basis. |
| CONTEXT_ROLE comparison is deterministic structural counting. | The context itself is the explicit observation basis for a role expectation. | `reconstructStructuralGap`. | IDs of matching-role context items are canonical; `observedCount === observedItemIds.length`; count at/above minimum returns `null`. |
| EBIND observations are structurally validated. | A fake or foreign proposal cannot serve as represented evidence. | `captureBinding`. | Exact shape, context, item, listed reference, disposition, trimmed rationale, and sealed `EBIND_` identity are required; otherwise `ERR_DECISION_STRUCTURAL_GAP_BINDING_INVALID`. |
| EBIND item/reference targets are unique in a basis. | Phase 5C3C must preserve the sealed Phase-5C2 one-binding-per-target invariant. | Binding ID and target-key sets. | Duplicate ID or distinct IDs for the same item/reference target fail `ERR_DECISION_STRUCTURAL_GAP_BASIS_INVALID`. |
| Irrelevant EBIND observations do not change `DGAP_`. | Only the expectation subject's represented observations belong to its derived body. | Relevant-binding filter and canonical binding-ID body. | Other-item bindings are validated but omitted from `observedBindingIds`. |
| DEPENDENCY comparison preserves direction. | A -> B is not B -> A. | Exact endpoint comparison. | Exact proposal returns `null`; reverse proposal does not satisfy but its ID is retained as relevant observation data. |
| CONTRADICTION proposals do not satisfy dependency expectations. | DREL proposal kinds have separate semantics. | Dependency-proposal filter. | Contradictions are omitted from dependency observation IDs and cannot satisfy. |
| DREL remains proposal data. | Structural consumption does not upgrade relation truth. | Sealed `assertStructuralRelationProposal` use. | Invalid/tampered DREL fails `ERR_DECISION_STRUCTURAL_GAP_RELATION_INVALID`; valid DREL is not relation truth. |
| `DGAP_` identity is deterministic. | The same represented derivation must have one derived-artifact identity. | `structural-gaps/identity.ts`. | SHA-256 tuple includes context, expectation, kind, and canonical body; `observedCount` is derived, not independently identity-bearing. |
| Structural gaps have no independent provenance. | Expectation provenance is already represented by `expectationId`; gap derivation must not add an ungoverned origin axis. | `StructuralGap` variants. | No provenance field exists on a gap. |
| Stored gap assertion is basis-bound. | Hash consistency is not derivation validity. | `assertStructuralGap(context, expectation, basis, gap)`. | It reconstructs against the exact basis; a supplied gap for a satisfying basis fails `ERR_DECISION_STRUCTURAL_GAP_INVALID`. |
| Stored gap arrays are already canonical. | Stored artifacts must not be silently repaired. | Stored assertion canonical-ID checks. | Reordered `observedBindingIds`, `observedItemIds`, or `observedRelationProposalIds` fail `ERR_DECISION_STRUCTURAL_GAP_INVALID`. |
| Basis and nested-artifact errors remain distinct from stored-gap errors. | A bad basis, EBIND, or DREL is not a bad stored gap. | Reconstruction before stored-gap validation in `assertStructuralGap`. | Basis wrapper/container: `BASIS_INVALID`; EBIND: `BINDING_INVALID`; DREL: `RELATION_INVALID`; bad stored gap: `INVALID`. |
| Phase 5C3C performs no authority, evaluator, detector, or graph work. | Gap reconstruction compares represented structure only. | `structural-gaps/reconstruct.ts` public API. | No reader, resolver, repository, payload, binder, evaluator, relation discovery, or graph traversal dependency exists. |
| Structural-gaps public surface excludes later decision concepts and remains generic. | The derived-gap module must not gain consequence or decision intelligence. | Export/import architecture gates in `structural-gaps/reconstruct.test.ts`. | No Career, Capability Core, matching, recommendation, or legacy-loop import; no Consequence, DecisionNeed, Priority, Score, Confidence, Ranking, Recommendation, HumanDecision, Action, Outcome, or Feedback export. |

## Phase 5C3D structural-consequence invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| A consequence requires a validated item-anchored gap and one explicit ordered dependency path. | A Gap or a dependency proposal alone cannot establish a structural consequence. | `structural-consequences/reconstruct.ts`. | No unordered relation inventory or graph discovery API exists. |
| Source gaps are operation-locally revalidated. | `DGAP_` hash consistency is not derivation validity or a portable certificate. | Sealed `assertStructuralGap` and `reconstructStructuralGap` calls. | Sealed Phase-5C3C context, expectation, basis, binding, and relation errors propagate unchanged. |
| Source-gap validation order preserves sealed error ownership. | An invalid expectation must fail before a gap basis is inspected. | Context capture, expectation capture/validation, then gap-basis capture. | Invalid expectation: `ERR_DECISION_STRUCTURAL_GAP_EXPECTATION_INVALID`; valid expectation then malformed gap basis/artifact preserves the matching Phase-5C3C error. |
| Only item-anchored gaps propagate. | A role-count gap has no uniquely represented missing item. | Source-anchor derivation. | Evidence gap anchors at `subjectItemId`; dependency gap at `dependentItemId`; context-role gap fails `ERR_DECISION_STRUCTURAL_CONSEQUENCE_SOURCE_NOT_ITEM_ANCHORED`. |
| Propagation consumes one explicit ordered DEPENDENCY path. | Relation ontology must not become graph discovery or relation truth validation. | `StructuralConsequencePropagationBasis`. | No reachability search, downstream discovery, shortest-path computation, ranking, or relation inference occurs. |
| Path direction follows prerequisite to dependent. | Stored DREL direction is `dependentItemId depends on prerequisiteItemId`. | Linear path validation. | First prerequisite equals source; each prior dependent equals next prerequisite; final dependent is `affectedItemId`. |
| Path topology is locally finite. | A supplied propagating path must not repeat its own relation or item. | Relation-ID and visited-item sets. | Empty path, wrong kind, discontinuity, duplicate relation ID, or repeated item fails `ERR_DECISION_STRUCTURAL_CONSEQUENCE_PATH_INVALID`; this is not a global acyclicity claim. |
| DREL failure differs from path failure. | A malformed/tampered relation is not a valid relation in bad topology. | Defensive nested relation capture then sealed relation assertion. | Invalid DREL: `ERR_DECISION_STRUCTURAL_CONSEQUENCE_RELATION_INVALID`; valid DREL with invalid topology: `ERR_DECISION_STRUCTURAL_CONSEQUENCE_PATH_INVALID`. |
| `DCONS_` identity is deterministic and path order is identity-bearing. | One source gap plus one explicit path must have a stable local derivation identity. | `structural-consequences/identity.ts`. | SHA-256 tuple contains schema, context, source gap, and ordered path IDs; different paths differ even if they share an affected item. |
| Consequence endpoint fields are derived. | The caller must not independently choose source or affected item. | Reconstructed source anchor and final dependent. | Stored source/affected/path body differing from reconstruction fails `ERR_DECISION_STRUCTURAL_CONSEQUENCE_INVALID`. |
| Stored consequence assertion is derivation-bound. | A self-consistent DCONS hash for another path must not be accepted. | `assertStructuralConsequence`. | Exact inputs reconstruct before stored representation is accepted; only an otherwise exact wrong ID yields `ERR_DECISION_STRUCTURAL_CONSEQUENCE_ID_MISMATCH`. |
| Returned consequences detach from caller input. | Later mutation of supplied gaps or relation proposals cannot alter the returned consequence. | Defensive capture plus detached return. | Contract tests mutate caller-owned DREL input after reconstruction. |
| Structural consequence is not a real-world effect or decision artifact. | Structural propagation must not acquire prediction, severity, priority, Decision Need, recommendation, or human decision semantics. | Closed `StructuralConsequence` fields and export gate. | No provenance, rationale, score, probability, confidence, severity, priority, recommendation, decision, action, outcome, or feedback field/export. |
| Structural-consequences production remains generic and adjacent. | Consequence derivation must not widen sealed predecessor barrels or import domain semantics. | `structural-consequences` module/export and import gates. | No Career, Capability Core, matching, recommendation, or legacy-loop import; `structural-findings` and `structural-gaps` exports remain unchanged. |

## Phase 5C4 validation-assembly invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| A validation assembly records derivational coherence only. | Revalidating structural derivations cannot establish truth, completeness, current authority, semantic verification, or decision readiness. | `validation-assembly/reconstruct.ts`. | No authority flag, score, recommendation, Decision Need, or human-decision field or API exists. |
| The Decision Context remains `NOT_RUN`. | Phase 5C4 produces a separate artifact rather than mutating or upgrading a `DecisionContextDraft`. | Sealed Phase-5B draft contract and closed assembly fields. | No `ValidatedDecisionContext` or `validationStatus: "VALIDATED"` state is constructed. |
| Empty assembly input is valid. | Phase 5C4 has no implicit completeness requirement. | Canonical assembly reconstruction. | Empty expectation and consequence arrays produce an empty assembly, not a completeness proof. |
| Each expectation result is reconstructed locally. | Caller-supplied `null` or gap content is not derivation authority. | Sealed `reconstructStructuralGap` and, for gaps, `assertStructuralGap`. | Matching canonical `null` becomes `NO_GAP`; matching canonical gap becomes `GAP`; disagreement fails `ERR_DECISION_VALIDATION_ASSEMBLY_RESULT_MISMATCH`. |
| `NO_GAP` is basis-scoped only. | A null Phase-5C3C result is neither truth nor global satisfaction. | Closed result union. | `NO_GAP` means only no StructuralGap under this explicit represented basis. |
| The assembly commits to the complete derivation basis. | A `DGAP_` commits only to the relevant Phase-5C3C gap body, not necessarily every supplied observation. | `StructuralValidationBasisDescriptor` plus assembly `contextId`. | EVIDENCE_BINDING and DEPENDENCY descriptors carry complete canonical supplied observation-ID inventories; CONTEXT_ROLE is kind-only because its canonical context observations are already bound by `contextId`. Canonical ID arrays are code-point sorted, duplicates reject, and a different descriptor changes `DVASM_`. |
| One expectation ID occurs at most once per assembly. | Different basis evaluations must not be silently merged. | Expectation-result identity set. | `ERR_DECISION_VALIDATION_ASSEMBLY_DUPLICATE_EXPECTATION`. |
| A consequence requires its exact assembled GAP source derivation. | A self-consistent `DCONS_` alone cannot establish assembly coherence. | Sealed `assertStructuralConsequence` plus source-result lookup. | Same expectation ID, canonical gap-basis descriptor, and gap ID are required; missing source fails `ERR_DECISION_VALIDATION_ASSEMBLY_CONSEQUENCE_SOURCE_MISSING`. |
| A GAP may have zero or multiple consequences. | Gap and consequence remain distinct structural artifacts. | Consequence collection. | No consequence is synthesized; distinct consequence IDs may share one GAP source. |
| Consequence IDs and expectation results are canonical. | Input order must not create a different assembly. | Code-point sorting before identity construction. | Duplicate consequence ID fails `ERR_DECISION_VALIDATION_ASSEMBLY_DUPLICATE_CONSEQUENCE`; input ordering does not change `DVASM_`. |
| One derivation occurrence uses one detached snapshot. | Caller mutation or a non-idempotent proxy must not make derivation input differ from the basis committed into identity. | Operation-local descriptor capture. | The same detached predecessor representations are reused for validation, reconstruction, descriptor construction, and IDs. |
| Phase-5B context error ownership is preserved. | A bad Decision Context is not a StructuralGap-context error. | Direct sealed `assertDecisionContextDraft` use. | `ERR_DECISION_CONTEXT_INVALID` and canonical Phase-5B errors, including `ERR_DECISION_CONTEXT_ID_MISMATCH`, propagate unchanged even for an empty assembly. |
| Stored assertion is derivation-bound. | A self-consistent `DVASM_` must not make a different assembly body valid. | `assertDecisionContextValidationAssembly`. | Reconstructed body mismatch fails `ERR_DECISION_VALIDATION_ASSEMBLY_INVALID`; otherwise exact body with wrong ID fails `ERR_DECISION_VALIDATION_ASSEMBLY_ID_MISMATCH`. |
| Phase 5C4 performs no authority or semantic operation. | Contract coherence is not current authority or semantic verification. | `validation-assembly` public API and import gate. | No reader, resolver, repository, payload, authority validator, binder, or evaluator dependency exists. |
| Validation-assembly production remains generic and closed. | Assembly infrastructure must not acquire domain or later decision concepts. | Public export/import gates. | No Career, Capability Core, matching, recommendation, legacy-loop import, or DecisionNeed/Score/Confidence/Priority/Recommendation/HumanDecision/Action/Outcome/Feedback export. |

## Phase 5D1 immutable revision-artifact invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| A revision is self-contained derivation state, not persistence authority. | Validated derivation cannot establish a persisted authority of record. | `revisions/contract.ts`. | A revision embeds its context, validation input, and validation assembly; no repository, persistence, authority, or head-selection API exists. |
| The three identities remain distinct. | Structural context, derivational coherence, and revision artifact identify different layers. | Sealed `DCTX_`, `DVASM_`, and `DREV_` identity contracts. | `DCTX_ != DVASM_`, `DVASM_ != DREV_`, and `DCTX_ != DREV_`; none establishes truth or persistence authority. |
| `DREV_` is deterministic over previous-revision representation, context identity, and assembly identity. | One canonical revision identity must not depend on input order, timestamps, or randomness. | `revisions/identity.ts`. | SHA-256 tuple is schema, `previousRevisionId`, `context.contextId`, and `validationAssembly.assemblyId`. |
| Root and child-shaped predecessor representations are structural only. | Phase 5D1 must not imply that a parent exists or select lineage state. | Previous-revision ID validation. | Root uses `null`; child shape must match `^DREV_[0-9A-F]{24}$`. A malformed non-null constructor input fails `ERR_DECISION_CONTEXT_REVISION_PREVIOUS_ID_INVALID`; malformed stored revision representation, including this field, fails `ERR_DECISION_CONTEXT_REVISION_INVALID`; no parent lookup occurs. |
| DREV identity is not the complete revision payload. | Identity-excluded predecessor representation can remain meaningful stored state. | Revision identity tuple and canonical embedded input. | A valid EBIND rationale change may preserve EBIND/DVASM/DREV identity while yielding a different complete revision payload; 5D1 does not resolve that future persistence conflict. |
| Embedded state is sufficient for local derivation revalidation. | `DVASM_` alone cannot reconstruct the inputs that generated it. | Embedded context, validation input, and validation assembly. | `assertDecisionContextDraft(context)` and `assertDecisionContextValidationAssembly(context, validationInput, validationAssembly)` require no external derivation artifact; this is not durable persistence or cold-restart proof. |
| Validation input is canonical before embedding in the revision artifact. | Non-semantic inventory order must not create alternate canonical revision bodies. | `canonicalValidationInput`. | Expectation validations sort by expectation ID; EBIND bindings by binding ID; dependency bases by relation-proposal ID; consequence validations by consequence ID. Duplicate validity remains governed by sealed predecessor/5C4 contracts. |
| Explicit propagation-path order is preserved. | An ordered DEPENDENCY path is semantic input to DCONS derivation. | Consequence propagation-basis canonicalization. | `DEPENDENCY_PATH.relationProposals` is not sorted. |
| Stored validation assembly is reconstructed canonically. | A revision cannot merely embed caller-owned, independently claimed DVASM state. | Sealed 5C4 assertion plus `assembleDecisionContextValidation`. | Canonical validation input reconstructs the stored assembly; incoherent or noncanonical body fails revision assertion. |
| One revision operation uses one detached snapshot. | Caller representation after capture must have zero authority over validation, revision construction, or canonical stored-revision comparison. | Revision preparation/capture path. | The detached captured context, validation input, and assembly are reused; assertion compares captured stored revision representation rather than rereading caller-owned nested state. |
| The draft remains `NOT_RUN`. | A revision artifact does not upgrade a structural context into a validated context. | Embedded sealed Phase-5B context. | No `ValidatedDecisionContext` or `validationStatus: "VALIDATED"` is constructed. |
| Stored body mismatch differs from deterministic ID mismatch. | A self-consistent DREV string cannot certify a different stored revision body. | `assertDecisionContextRevision`. | Noncanonical or body-mismatching revision fails `ERR_DECISION_CONTEXT_REVISION_INVALID`; otherwise exact valid body with only wrong ID fails `ERR_DECISION_CONTEXT_REVISION_ID_MISMATCH`. |
| Sealed predecessor errors retain ownership. | A revision wrapper must not erase the context or derivation contract that owns a failure. | Safe top-level capture followed by sealed assertions. | Meaningful Phase-5B and Phase-5C4/predecessor errors remain observable after safe revision representation capture. |
| Revisions remain generic and closed. | The revision artifact must not become a Career/repository/decision-intelligence API. | `revisions` public and import gates. | No Career, Recruiting, Capability Core, matching, recommendation, authority, repository, binder, evaluator, DecisionNeed, priority, score, confidence, recommendation, human-decision, action, outcome, or feedback dependency/export exists. |

## Phase 5D2A repository-bound immutable persistence invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Interface conformance differs from governance guarantee. | The public repository type cannot prove every third-party implementation preserves 5D2A behavior. | `DecisionContextRevisionRepository` interface versus shipped in-memory implementation. | The interface defines supported read/factory shape and no raw-writer member; the shipped `InMemoryDecisionContextRevisionRepository` enforces the documented invariant. |
| The bound persister is the supported shipped write capability. | Storage machinery must not bypass validation, parent integrity, reread, and exact-equality checks. | `revision-persistence` public barrel and shipped in-memory repository. | Public surface is `createDecisionContextRevisionPersister() -> persist(revision)`; the shipped in-memory `#writeRevision` is runtime-private. |
| Authority of record is operation-relative, not truth. | Repository selection cannot establish semantic correctness, current producer authority, current decision state, or branch preference. | Successful shipped in-memory bound `persist(...)` only. | A return means this bound repository selected this exact complete artifact for this DREV during this operation; it does not imply head/latest/active, truth, or recommendation. |
| One persistence occurrence uses one pristine expected snapshot. | Writer mutation must not alter the authority state used for post-write comparison. | `persist(...)` capture path. | Caller revision is captured and sealed-asserted once; the writer receives a detached clone and the reread compares against pristine expected state. |
| A root performs no parent lookup. | A root is not a claim of globally first, only, active, or preferred root. | `previousRevisionId === null` branch. | Valid roots write directly after sealed revision validation. |
| A child requires only immediate referential integrity. | 5D2A must prevent dangling direct predecessors without claiming full lineage validity. | One `getRevisionById(previousRevisionId)` before child write. | Missing parent fails `ERR_DECISION_CONTEXT_REVISION_PARENT_NOT_FOUND`; malformed, invalid, or ID-mismatched parent fails `ERR_DECISION_CONTEXT_REVISION_PARENT_INVALID`; no ancestry traversal occurs. |
| Forks and no-change children are valid. | Referential integrity is not branch policy or semantic-change detection. | Repository identity and child validation. | Multiple children may name one parent, and a child may retain parent context/assembly IDs. No head/latest/current/active/superseded or SemanticChange ontology exists. |
| Immutable replay compares complete artifacts, not DREV alone. | Identity-excluded state remains meaningful complete record state. | In-memory immutable writer and complete-data equality. | Exact same-ID replay succeeds; same DREV with divergent payload fails `ERR_DECISION_CONTEXT_REVISION_IMMUTABLE_CONFLICT`. EBIND rationale can be identity-excluded yet conflict-relevant. |
| Write success is insufficient without exact reread. | Authority of record requires repository-selected complete state, not merely a write attempt. | Post-write `getRevisionById(expected.revisionId)`. | Missing, malformed, invalid, wrong-ID, or divergent reread after write success fails `ERR_DECISION_CONTEXT_REVISION_PERSISTENCE_INVALID`; underlying writer failures may propagate. |
| Repository reads and persistence returns are detached. | Callers must not mutate repository state through a returned artifact. | In-memory read/return boundaries. | `getRevisionById(...)` and successful `persist(...)` return detached complete revisions. |
| 5D2A remains generic and in-memory only. | Generic Core must not acquire Career/database ontology or claim durability. | `revision-persistence` import/public gates. | No Career, Capability Core, adapters, Drizzle, Postgres, database, frontend, or later-decision imports/exports; no process-restart or database guarantee exists. |

## Phase 5D2B durable PostgreSQL persistence invariants

| Invariant | Why it exists | Enforcement point | Consequence |
| --- | --- | --- | --- |
| PostgreSQL is an adapter, not a Decision Core dependency. | The generic kernel must remain database and Career free. | `lib/decision-adapters/revision-persistence/**` dependency direction. | `decision-adapters/revision-persistence -> decision-core/revision-persistence -> decision-core/revisions`; no PostgreSQL, Drizzle, Career DB, or frontend import enters `lib/decision-core/**`. |
| The adapter receives configured database composition. | Repository adapter must not become a global database bootstrapper. | `PostgresDecisionContextRevisionRepository` constructor. | No `DATABASE_URL` read, pool creation, table/database creation, or migration execution occurs in production adapter code. |
| The physical table is minimal and fork-capable. | Immutable record identity and immediate references need no branch or lifecycle ontology. | `decision_context_revisions` Drizzle descriptor. | `revision_id TEXT PRIMARY KEY`; nullable non-unique `previous_revision_id` self-FK with non-cascading delete; `payload JSONB NOT NULL`; no timestamps, head/latest/current/active/superseded, branch, revision number, or payload hash. |
| Physical and embedded identity must agree. | A durable row has both physical key columns and embedded artifact identity. | PostgreSQL read capture. | Requested ID equals row ID where supplied; `row.revision_id == payload.revisionId`; `row.previous_revision_id == payload.previousRevisionId`; violation is `ERR_DECISION_CONTEXT_REVISION_POSTGRES_RECORD_INVALID`. |
| Durable reads assert exact persisted state. | A read must not replace corrupted/noncanonical stored representation with a reconstructed artifact. | `capturePersistedRevision`. | `READ != RECONSTRUCT != REPAIR`; detached JSONB payload passes sealed revision assertion before detached return. |
| JSONB object-key normalization is not semantic divergence. | PostgreSQL may reorder object keys while preserving structural data. | Targeted Phase-5C4 recursive structural equality. | Objects are key-set/value-sensitive and key-order-insensitive; arrays remain order- and length-sensitive; no 5C4 artifact, identity, API, or derivation contract changed. |
| Immutable insertion is database-race safe. | Process-local serialization cannot govern independent clients. | `INSERT ... ON CONFLICT DO NOTHING ... RETURNING` plus conflict-race reread. | Exact complete winner is idempotent; divergent complete winner is `ERR_DECISION_CONTEXT_REVISION_IMMUTABLE_CONFLICT`; no update, overwrite, repair, or payload-hash criterion exists. |
| Postgres race reread differs from authority reread. | Physical race resolution and operation authority verification answer different questions. | Adapter writer versus sealed 5D2A persister. | Conflict reread identifies the physical winner; final 5D2A reread establishes exact complete state for successful operation return. |
| Immediate-parent integrity has two layers. | Application validity and physical referential integrity are distinct. | Sealed 5D2A parent read plus PostgreSQL self-FK. | Missing visible parent is `ERR_DECISION_CONTEXT_REVISION_PARENT_NOT_FOUND`; FK blocks physical dangling row; neither traverses ancestry or proves full lineage. |
| Forks and no-change children remain valid. | Referential integrity is not branch selection or semantic-change detection. | Non-unique self-FK and sealed child semantics. | Multiple children may name one parent; child context/assembly IDs may equal parent IDs; no head/latest/current/active/superseded or branch-rank ontology exists. |
| Durable scope is bounded. | Client reconstruction is not universal failure recovery. | Isolated PostgreSQL integration tests. | Tests prove database-backed survival across repository/client reconstruction and durable parent use, not OS-process crash, machine restart, backup, replication, or disaster recovery. |
| Production schema testing follows the production descriptor. | A handwritten test DDL can drift from infrastructure schema. | Integration schema metadata checks and descriptor-derived isolated DDL. | Table name, columns, PK, FK, nullability, uniqueness, and delete behavior remain observable without production bootstrapping. |

## Phase 5D3 read-only revision-lineage invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Lineage reconstruction binds only an exact reader method. | Traversal must not obtain write or persistence capability by composition. | `revision-lineage/reconstruct.ts` reader capture. | Only own function-valued data-property `getRevisionById` is accepted; accessors or extra own capabilities fail `ERR_DECISION_CONTEXT_REVISION_LINEAGE_READER_INVALID`. |
| Start representation is checked before read. | A malformed caller ID must not direct repository access. | `reconstruct(startRevisionId)`. | Non-DREV shape fails `ERR_DECISION_CONTEXT_REVISION_LINEAGE_START_ID_INVALID` before any reader invocation. |
| Each node is sealed-valid and requested-ID exact. | A generic reader must not redirect the explicit predecessor path with arbitrary returned state. | Detached capture plus `assertDecisionContextRevision`. | Malformed/noncanonical revision, malformed predecessor representation, or wrong returned ID fails `ERR_DECISION_CONTEXT_REVISION_LINEAGE_REVISION_INVALID`; repository errors remain observable. |
| Lineage read validity does not establish persistence authority. | A generic reader proves returned revision shape and canonical validity, not how records were obtained or persisted. | 5D3 binds only `getRevisionById` and has no persister or repository-authority operation. | `SEALED-VALID READER RESULT != PERSISTENCE AUTHORITY`; repository-selected authority of record remains owned by the sealed 5D2A/5D2B paths. |
| A root is explicit null predecessor only. | Missing data cannot be silently reclassified as lineage termination. | Iterative predecessor traversal. | First missing record is `ERR_DECISION_CONTEXT_REVISION_LINEAGE_START_NOT_FOUND`; later missing record is `ERR_DECISION_CONTEXT_REVISION_LINEAGE_PREDECESSOR_NOT_FOUND`; no partial result exists. |
| Result ordering is predecessor order. | Revisions contain no timestamp or temporal relation. | Reverse capture sequence before detached return. | Returned array is `ROOT -> ... -> START`; it is not chronological, wall-clock, or causal order. |
| Repeated requested IDs fail before reread. | A compatible but hostile reader must not induce unbounded traversal. | Operation-local visited-ID set checked and added before each reader call. | Repeated ID fails `ERR_DECISION_CONTEXT_REVISION_LINEAGE_CYCLE`; this is not semantic or causal cycle analysis. |
| Returned lineages and operations are isolated. | Repository/caller mutation and concurrent operations must not share traversal state. | Detached revision capture, detached lineage return, local visited set and reverse sequence. | Returned state is detached, not promised deep-frozen; no cache or shared accumulator exists. |
| Lineage remains a narrow read model. | A predecessor path must not become a new authority, branch, or decision ontology. | Closed public type/export/import gates. | No `DLINE_`, persisted lineage artifact, authority certificate, head/latest/current/active, branch/descendant API, semantic-change, causation, DecisionNeed, recommendation, human-decision, action, outcome, feedback, Career, Capability Core, PostgreSQL, Drizzle, or adapter dependency exists. |

## Phase 6A human-owned assessment-request invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| The artifact records a human-owned normative frame only. | Evidence-backed state, human normative ownership, later proposals, and human decision remain separate axes. | `assessment-request/types.ts` and contract tests. | `HUMAN ASSESSMENT REQUEST != DECISION NEED != ASSESSMENT != RECOMMENDATION != HUMAN DECISION`. |
| Stored shape is exact and closed. | A request must not acquire hidden authority, execution, or temporal state. | Defensive exact capture in `assessment-request/contract.ts`. | Only the documented nine top-level fields are valid; extra/missing/accessor/symbol/non-enumerable representation fails stored-invalid. |
| Human origin is declared, not authenticated. | A request must not imply identity-provider, permission, signature, evidence, or decision semantics. | `requestedBy` structural validation. | `origin` is exactly `HUMAN_INPUT`; trimmed nonempty `actorId` is required, with no auth or lookup operation. |
| Revision references are shape-only. | A request must not silently bind to a repository-selected or current revision. | `^DREV_[0-9A-F]{24}$` validation only. | No repository read, revision assertion, persistence check, lineage traversal, head/latest/current selection, or producer-authority resolution occurs. |
| Item references are shape-only. | Request-level category declarations must not be misrepresented as context evidence. | `^DCI_[0-9A-F]{24}$` validation only. | No existence, revision-membership, or actual-role validation occurs. |
| Selection inventories are set-like and category-exclusive. | Caller order must not create alternate representations, while overlapping declared roles remain invalid. | Per-category canonical sorting plus cross-category checks. | Code-point sort on construction; duplicates, overlaps, and question reuse fail `ERR_DECISION_ASSESSMENT_REQUEST_DUPLICATE_SELECTION`. |
| Empty inventories remain valid. | Phase 6A is request representation, not an executable operation. | Constructor and assertion contract. | Options, objectives, and constraints may all be empty; no minimum count or readiness claim exists. |
| DAREQ commits exactly the declared canonical frame. | Identity must be deterministic without becoming authority. | SHA-256 over the documented JSON tuple. | Actor, revision, question, and each selection inventory independently affect `DAREQ_`; no timestamp, randomness, UUID, provider, or model metadata participates. |
| Construction and stored assertion have distinct duties. | Canonicalizing input must not become stored-artifact repair. | `createDecisionAssessmentRequest` versus `assertDecisionAssessmentRequest`. | Construction trims/sorts/detaches valid input; assertion rejects untrimmed actor IDs, noncanonical order, duplicates, and malformed state. |
| The module stays generic and closed. | A declarative request cannot become a domain-specific assessment engine. | Closed public surface and import gates. | No Career, Recruiting, Capability Core, matching, legacy loop, frontend, PostgreSQL, Drizzle, adapter, repository, evaluator, assessment, recommendation, scoring, or decision dependency/export exists. |

## Phase 6B revision-bound assessment-basis invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Reader composition is exact and read-only. | Binding a basis must not obtain repository write or authority capability. | `assessment-basis/contract.ts` reader capture. | Only one own enumerable data-method `getRevisionById` is accepted; extra/accessor/symbol/non-enumerable/missing/null/array/primitive dependencies fail `ERR_DECISION_ASSESSMENT_BASIS_READER_INVALID`. |
| Request state is captured before the reader await. | Caller mutation must not redirect the revision read, selections, owner, or identity. | `bind(...)` defensive capture then sealed 6A assertion. | Invalid/hostile request fails `ERR_DECISION_ASSESSMENT_BASIS_REQUEST_INVALID` before a reader call. |
| Returned revision is captured once and sealed-asserted. | A reader return is not trusted, stable, or persistence proof. | Detached revision capture plus `assertDecisionContextRevision(...)`. | Malformed/noncanonical/wrong-ID revision fails `ERR_DECISION_ASSESSMENT_BASIS_REVISION_INVALID`; later external mutation cannot affect the basis. |
| Requested and returned revision IDs are exactly equal. | A reader must not redirect a request to another sealed revision. | Bound request/revision comparison. | Returned `revisionId !== request.revisionId` fails `ERR_DECISION_ASSESSMENT_BASIS_REVISION_INVALID`; no current/head/latest selection occurs. |
| Question and selections bind membership and declared role only. | Context presence and role must not become support, importance, truth, or readiness. | Context item lookup and role checks. | Missing IDs fail `ERR_DECISION_ASSESSMENT_BASIS_ITEM_NOT_FOUND`; role mismatch fails `ERR_DECISION_ASSESSMENT_BASIS_ROLE_MISMATCH`. Question also equals `context.decisionQuestionId`. |
| Empty option, objective, and constraint inventories remain valid. | A bound basis is not assessment readiness. | Membership/role loop. | All inventories may be empty when the question binds. |
| `DABAS_` commits complete canonical request and revision state. | `DREV_` does not identify every meaningful complete revision payload value. | Private recursive structural canonicalizer and SHA-256 identity. | Object key insertion order is non-semantic; array order is preserved; same DREV with different complete semantic-binding rationale yields a different `DABAS_`. |
| Stored assertion is self-contained and non-repairing. | Stored basis validity cannot depend on a repository or repair hostile state. | `assertDecisionAssessmentBasis(...)`. | Exact five fields, embedded sealed assertions, ID equality, membership/role checks, and recomputed identity are required; malformed body fails `ERR_DECISION_ASSESSMENT_BASIS_INVALID`, otherwise-well-formed wrong ID fails `ERR_DECISION_ASSESSMENT_BASIS_ID_MISMATCH`. |
| Returned basis is detached and the module remains generic. | Basis construction must not share mutable caller/reader state or acquire domain/later-phase semantics. | Detached construction and import/public-surface gates. | No Career, Recruiting, Capability Core, adapters, repository writer, lineage traversal, producer-authority resolution, assessment, Decision Need, recommendation, score/rank, or human-decision API exists. |

## Phase 6C semantic-assessment-proposal invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Evaluator composition is exact and bound once. | Semantic evaluation must not acquire hidden capability or be redirected after construction. | `assessment-proposal/contract.ts` evaluator capture. | Exactly one own enumerable data-method `evaluate`; hostile forms fail `ERR_DECISION_ASSESSMENT_PROPOSAL_EVALUATOR_INVALID`. |
| Basis and provenance are captured before evaluation. | Caller mutation must not redirect basis, selected targets, proposal provenance, or identity. | `propose(...)` defensive capture and sealed basis assertion. | Invalid basis fails `...BASIS_INVALID`; invalid provenance fails `...PROVENANCE_INVALID`; evaluator is not called. |
| Evaluator input and output are detached. | Evaluator/caller-owned mutable memory cannot control constructed proposal state. | Detached input plus defensive output capture. | Returned proposal remains stable after evaluator input/output mutation; detached does not mean deep-frozen. |
| Only human-selected targets are admitted. | Revision membership must not silently become normative relevance. | Embedded request selected inventories. | Valid-shaped unselected option fails `...OPTION_NOT_SELECTED`; valid-shaped unselected objective/constraint fails `...CRITERION_NOT_SELECTED`. |
| Dispositions are closed and relations are optional. | Assessment representation must not become score, readiness, or synthetic completeness. | `DECISION_ASSESSMENT_DISPOSITIONS` and output validation. | Exactly four dispositions; zero/partial relations valid; no missing relation becomes `UNDETERMINED`. |
| Target pairs are unique and canonical. | One represented option/criterion relation must not silently contain competing outputs. | Pair set and code-point canonical ordering. | Duplicate pair fails `...DUPLICATE`; equivalent evaluator order yields canonical same-order assessments. |
| Rationale is canonical and identity-bearing. | Phase 6C identifies complete represented proposal state. | Trimmed evaluation capture and DASPR tuple. | Empty/untrimmed rationale fails evaluation/stored assertion; wording change changes `DASPR_`. |
| `DASPR_` commits complete basis, provenance, and assessments. | Proposal identity must be deterministic without becoming authority or truth. | Private recursive canonicalizer and SHA-256 identity. | Object insertion order is non-semantic, array order preserved, and every admitted assessment axis participates. |
| Stored assertion is self-contained and non-repairing. | Stored proposal validity must not invoke semantic dependencies or repair body state. | `assertDecisionAssessmentProposal(...)`. | Hostile/noncanonical/embedded-invalid/target-invalid/duplicate body fails `...INVALID`; otherwise-valid stale ID fails `...ID_MISMATCH`. |
| Proposal remains a bounded model proposal. | Semantic assessment must not become recommendation or decision ontology. | Closed public surface and import gates. | No recommendation, Decision Need, human decision, score, rank, priority, weight, confidence, persistence, lineage, current-selection, Career, or provider implementation exists. |

## Phase 6D recommendation-proposal invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Generator composition is exact and bound once. | Recommendation proposal must not acquire hidden capability or be redirected after construction. | `recommendation-proposal/contract.ts` generator capture. | Exactly one own enumerable data-method `recommend`; hostile forms fail `ERR_DECISION_RECOMMENDATION_PROPOSAL_GENERATOR_INVALID`. |
| Assessment proposal and provenance are captured before generation. | Caller mutation must not redirect the sealed predecessor, target inventories, provenance, or identity. | `propose(...)` defensive capture and sealed assessment-proposal assertion. | Invalid assessment proposal fails `...ASSESSMENT_PROPOSAL_INVALID`; invalid provenance fails `...PROVENANCE_INVALID`; generator is not called. |
| Generator input and output are detached. | Generator/caller-owned mutable memory cannot control constructed recommendation proposal state. | Detached input plus defensive output capture. | Returned proposal remains stable after generator input/output mutation; detached does not mean deep-frozen. |
| Recommendation targets are both selected and assessment-represented options. | Revision membership and human selection must not be conflated with represented assessment. | Embedded selected-option inventory and assessment option IDs. | Valid-shaped unselected option fails `...OPTION_NOT_SELECTED`; selected option absent from all assessments fails `...OPTION_NOT_ASSESSED`. |
| Assessment disposition does not define recommendation policy. | An assessment relation is neither a recommendation nor a preference/ranking rule. | Target admission intentionally ignores disposition. | `ALIGNED`, `PARTIALLY_ALIGNED`, `MISALIGNED`, and `UNDETERMINED` can each satisfy structural admission; no score, priority, rank, rejection, or winner state exists. |
| Zero, partial, and multiple recommendations remain valid. | The contract represents admitted proposal relations without completeness, rejection, or ranking claims. | Generator-output validation. | Empty output is valid; multiple targets are valid and no absent target receives synthesized rejection state. |
| Recommendation targets are unique and canonically ordered. | One option cannot silently contain competing recommendation rationales, and generator output order must not affect identity. | Target set and code-point option-ID ordering. | Duplicate option fails `...DUPLICATE`; valid output is ordered deterministically by `optionItemId`. |
| Rationale is canonical and identity-bearing. | Phase 6D identifies complete represented recommendation proposal state. | Trimmed recommendation capture and DRECP tuple. | Empty/untrimmed rationale fails generation/stored assertion; wording change changes `DRECP_`. |
| `DRECP_` commits complete assessment proposal, provenance, and recommendations. | Proposal identity must be deterministic without becoming truth, correctness, optimality, or decision authority. | Private recursive canonicalizer and SHA-256 identity. | Object insertion order is non-semantic, predecessor array order is preserved, and every admitted recommendation axis participates. |
| Stored assertion is self-contained and non-repairing. | Stored validity must not invoke a generator or repair body state. | `assertDecisionRecommendationProposal(...)`. | Hostile/noncanonical/embedded-invalid/target-invalid/duplicate body fails `...INVALID`; otherwise-valid stale ID fails `...ID_MISMATCH`. |
| Proposal remains a bounded generic model-proposal artifact. | Recommendation proposal must not become a human decision, recommendation policy, or domain/persistence mechanism. | Closed public surface and import gates. | No Decision Need, score, weight, confidence, probability, priority, rank, winner, preference, action, outcome, feedback, learning, persistence, lineage, current-selection, Career, provider, or recommendation-policy implementation exists. |

## Phase 6E proposal-coherence-validation invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Input is one sealed complete recommendation proposal. | Traceability must derive from one exact predecessor representation rather than caller-selected fragments. | `proposal-coherence/contract.ts` defensive capture plus `assertDecisionRecommendationProposal(...)`. | Invalid or hostile predecessor fails `ERR_DECISION_PROPOSAL_COHERENCE_RECOMMENDATION_PROPOSAL_INVALID`. |
| One trace exists for every recommendation and no other option. | Coherence is limited to recommendation-to-represented-assessment traceability. | Deterministic trace reconstruction. | Trace inventory must exactly match recommendation inventory; missing or extra trace fails `ERR_DECISION_PROPOSAL_COHERENCE_INVALID`. |
| Each trace criterion inventory is exact and canonical. | Trace data must represent all and only embedded assessment criteria for that option without repair or implicit completeness policy. | Assessment filtering plus code-point ordering. | Wrong, missing, extra, duplicate, or reordered criterion state fails `ERR_DECISION_PROPOSAL_COHERENCE_INVALID`. |
| Disposition and rationale are not coherence policy. | Assessment representation must not become support, justification, correctness, readiness, or rejection state. | Trace reconstruction selects only option/criterion IDs. | `ALIGNED`, `PARTIALLY_ALIGNED`, `MISALIGNED`, and `UNDETERMINED` are all traced identically; no interpretation field or error exists. |
| Zero, partial, and multiple traces remain valid. | Structural traceability is not completeness, readiness, or ranking. | Deterministic reconstruction from recommendation inventory. | Zero recommendations produce zero traces; assessed but unrecommended options produce no trace; multiple recommendations produce one trace each. |
| `DPCV_` commits complete recommendation-proposal state and canonical traces. | Trace summary alone cannot identify all represented predecessor state. | Private recursive canonicalizer and SHA-256 identity. | Object insertion order is non-semantic, predecessor arrays remain preserved, and disposition/rationale/provenance/human-frame/inventory changes alter `DPCV_` even when traces do not. |
| Stored assertion is self-contained and non-repairing. | Stored validation must not invoke dependencies or rewrite the supplied trace body. | `assertDecisionProposalCoherenceValidation(...)`. | Hostile/noncanonical/predecessor-invalid/trace-mismatch body fails `...INVALID`; otherwise-valid stale ID fails `...ID_MISMATCH`. |
| Validation remains deterministic structural traceability only. | Coherence must not become semantic correctness, authority, or decision state. | Exact public surface and generic import gates. | No model, provider, evaluator, generator, human actor, reader, repository, persister, lineage, authority resolver, truth, support, score, rank, Decision Need, human decision, action, outcome, feedback, or status API exists. |

## Phase 7A human-decision-declaration invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Input is one sealed complete DPCV plus exact three-field human input. | A declaration must bind one complete predecessor state without caller-owned mutable fragments. | `human-decision/contract.ts` defensive capture plus `assertDecisionProposalCoherenceValidation(...)`. | Invalid predecessor fails `ERR_DECISION_HUMAN_DECISION_PROPOSAL_COHERENCE_INVALID`; hostile input representation fails `ERR_DECISION_HUMAN_DECISION_INPUT_INVALID`. |
| Human choice admission uses complete revision `OPTION` membership only. | The model-proposal path must not narrow the human decision space. | Embedded revision context item lookup and role check. | Chosen ID must be DCI-shaped, present, and role `OPTION`; malformed, absent, or wrong-role state fails its owned option error. |
| Assessment selection, assessment, recommendation, and trace are not choice admission gates. | `HUMAN ASSESSMENT SELECTION != ASSESSMENT != RECOMMENDATION != COHERENCE TRACE != HUMAN DECISION ADMISSIBILITY`. | No access to those inventories in option admission. | An actual revision `OPTION` remains admissible even if omitted from every Phase 6 model-path inventory. |
| Positive selection is nonempty, unique, and order-canonical. | 7A represents one or more explicit choices without inventing defer/reject or preference ordering. | Choice inventory validation and code-point canonicalization. | Empty input fails `...INPUT_INVALID`; duplicates fail `...DUPLICATE_OPTION`; noncanonical stored order fails `...INVALID`. |
| Decision actor is declared human ownership only. | Human input must not be confused with identity proof or authorization. | Exact actor capture with `origin: "HUMAN_INPUT"` and trimmed nonempty ID. | Invalid actor fails `...ACTOR_INVALID`; `DECISION ACTOR != ASSESSMENT REQUESTER`. |
| Rationale is optional but canonical when present. | Human rationale is represented text, not proof or semantic truth. | Null-or-trimmed-nonempty validation. | Whitespace/invalid constructor rationale fails `...RATIONALE_INVALID`; untrimmed stored rationale fails `...INVALID`. |
| `DHDEC_` commits complete DPCV, actor, canonical choices, and rationale. | Human declaration identity must retain complete represented predecessor state without becoming truth or authority. | Private recursive canonicalizer and SHA-256 identity. | Object insertion order is non-semantic; predecessor state, actor, choice-set, or rationale changes alter `DHDEC_`. |
| Stored assertion is self-contained and non-repairing. | Stored decision validity must invoke no external dependency and must not rewrite submitted state. | `assertHumanDecisionDeclaration(...)`. | Hostile/noncanonical/predecessor-invalid/body-invalid state fails `...INVALID`; otherwise-valid stale ID fails `...ID_MISMATCH`. |
| Hostile representations are rejected safely and returned state is detached. | Accessor and hidden mutable state must not become authority in declaration construction/assertion. | Descriptor-based recursive capture and detached return. | Accessors, symbols, hidden fields, sparse/custom arrays, and cycles reject without getter execution; detached does not mean deep-frozen. |
| Human declaration remains bounded generic decision state. | It must not absorb Career ontology, recommendation policy, action, outcome, feedback, or persistence authority. | Exact public surface and predecessor-only import gate. | No ACCEPT/REJECT/DEFER state, score/rank/priority, model/provider, action/outcome/feedback, persistence, or legacy Career decision coupling exists. |

## Phase 8A1 decision-bound-action-intent invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Input is one sealed complete human decision plus exact four-field local input. | Intended operation state must bind one exact decision without lower-phase bypass or mutable fragments. | `action-intent/contract.ts` defensive capture plus `assertHumanDecisionDeclaration(...)`. | Invalid predecessor fails `ERR_DECISION_ACTION_INTENT_HUMAN_DECISION_INVALID`; hostile input representation fails `ERR_DECISION_ACTION_INTENT_INPUT_INVALID`. |
| Operationalized options are a nonempty canonical subset of human choices. | The model cannot narrow pre-decision human choice, while operationalization cannot expand beyond a completed human declaration. | Membership only in `humanDecisionDeclaration.chosenOptionItemIds` and code-point canonicalization. | Malformed ID, unchosen ID, duplicate, or empty input fails its owned option/input error; actual revision membership alone is insufficient. |
| No lower-phase model-path inventory is an operationalization gate. | `MODEL PROPOSAL SPACE != HUMAN DECISION SPACE` and `ACTION INTENT SCOPE ⊆ HUMAN DECISION CHOICE SET` are distinct governance boundaries. | No inspection of assessment request, relations, recommendations, traces, revision roles, or authority dependencies. | An option chosen by the human is admissible regardless of model-path inclusion; an actual revision option not human-chosen is rejected. |
| Intent declarer is declared human input only. | Declaration is not identity proof, authorization, signature, permission, or truth. | Exact `HUMAN_INPUT` actor capture with trimmed nonempty ID. | Invalid actor fails `...ACTOR_INVALID`; no equality with decision actor is required. |
| Operation description is opaque and canonical. | The generic core must not create an executable command language. | Trimmed nonempty string capture only. | Invalid/blank description fails `...OPERATION_INVALID`; no action type, target, assignee, executor, schedule, or expected-effect field exists. |
| Rationale is optional but canonical when present. | Represented rationale is neither intent, proof, truth, authorization, nor execution. | Null-or-trimmed-nonempty validation. | Invalid constructor rationale fails `...RATIONALE_INVALID`; untrimmed stored rationale fails `...INVALID`. |
| Multiple intents are not globally constrained. | One decision can exist before, beside, or under multiple operationalizations. | No repository, lookup, global state, or cross-intent inspection. | Zero, one, multiple, and overlapping independently declared subsets are not restricted by this slice. |
| `DAINT_` commits complete human-decision and local intent state. | Identity must retain represented predecessor state without becoming authorization, execution, truth, or persistence authority. | Private recursive canonicalizer and SHA-256 identity. | Object insertion order is non-semantic; predecessor state, actor, option subset, operation text, or rationale changes alter `DAINT_`. |
| Stored assertion is self-contained and non-repairing. | Stored intended-operation validity must not call external dependencies or rewrite the body. | `assertDecisionActionIntent(...)`. | Hostile/noncanonical/predecessor-invalid/body-invalid state fails `...INVALID`; otherwise-valid stale ID fails `...ID_MISMATCH`. |
| Hostile representations are rejected safely and returned state is detached. | Hidden/accessor state must not affect intended-operation construction/assertion. | Descriptor-based recursive capture and detached return. | Accessors, symbols, hidden fields, sparse/custom arrays, cycles, including nested human-decision state, reject without getter execution; detached does not mean deep-frozen. |
| Action Intent remains pre-commitment and pre-action state. | Intention must not become responsibility, occurrence, outcome, or feedback. | Exact closed artifact/public surface and generic import gate. | No commitment/action/outcome/feedback/learning/status/temporal/authorization API or legacy Career action coupling exists. |

## Phase 8A2 human-commitment invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Input is one sealed complete Action Intent plus exact two-field local input. | Commitment must bind one complete represented intended operation without lower-predecessor bypass or mutable fragments. | `human-commitment/contract.ts` defensive capture plus `assertDecisionActionIntent(...)`. | Invalid predecessor fails `ERR_DECISION_HUMAN_COMMITMENT_ACTION_INTENT_INVALID`; hostile input fails `ERR_DECISION_HUMAN_COMMITMENT_INPUT_INVALID`. |
| Commitment has no independent option scope. | Action Intent owns operationalization scope; commitment must not reinterpret, expand, or shrink it. | Exact six-field artifact excludes option IDs and operation-description duplicate. | A partial commitment requires a separate narrower Action Intent before one complete commitment artifact can be created. |
| Commitment actor is declared human input only. | Declared commitment must not become authenticated identity, authorization, organizational role, legal accountability, assignment, or execution authority. | Exact `HUMAN_INPUT` actor capture with trimmed nonempty ID; no equality check against predecessor actors. | Invalid actor fails `...ACTOR_INVALID`; decision actor, Action Intent declarer, commitment actor, and future Action actor are independent semantic role positions. No actor-ID equality or inequality is required or inferred. |
| One artifact has one actor; commitments are not globally constrained. | Multi-actor aggregation requires distinct future semantics. | Exact actor shape; no repository, lookup, global state, or cross-commitment inspection. | One Action Intent may have zero, one, or multiple independent commitments; `ONE ACTION INTENT != ONE HUMAN COMMITMENT`. |
| Rationale is optional but canonical when present. | It represents only the declared actor's reason for committing, not commitment, authorization, proof, plan, expected outcome, or truth. | Null-or-trimmed-nonempty validation. | Invalid constructor rationale fails `...RATIONALE_INVALID`; untrimmed stored rationale fails `...INVALID`. |
| `DHCOM_` commits complete Action Intent and local commitment state. | Identity retains complete represented predecessor state without becoming authorization, assignment, execution, truth, or persistence authority. | Private recursive canonicalizer and SHA-256 identity. | Object insertion order is non-semantic; predecessor state, actor, or rationale changes alter `DHCOM_`. |
| Stored assertion is self-contained and non-repairing. | Stored commitment validity must invoke no external dependency and must not rewrite submitted state. | `assertHumanCommitment(...)` calls only `assertDecisionActionIntent(...)`. | Hostile/noncanonical/predecessor-invalid/body-invalid state fails `...INVALID`; otherwise-valid stale ID fails `...ID_MISMATCH`. |
| Hostile representations are rejected safely and returned state is detached. | Hidden/accessor state must not become commitment authority. | Descriptor-based recursive capture and detached return. | Accessors, symbols, hidden fields, cycles, and nested sparse/custom arrays reject without getter execution; detached does not mean deep-frozen. |
| Human Commitment remains pre-action state. | Declared commitment must not become responsibility, ownership, accountability, authorization, assignment, action, execution, completion, outcome, feedback, learning, or persistence authority. | Exact closed artifact/public surface and predecessor-only import gate. | No responsibility/ownership/accountability/assignment/authorization/action/outcome/feedback/learning/temporal API or legacy Career coupling exists. |

## Phase 8B action-occurrence-claim invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Claim is standalone exact two-field input and five-field artifact. | Occurrence reporting must not become a mandatory continuation of decision/intention state. | Exact closed `action-occurrence-claim` input/artifact. | No Decision, Intent, Commitment, revision, assessment, recommendation, coherence, or optional predecessor field exists. |
| Source union is only `HUMAN_INPUT | AUTHORITATIVE_STATE`. | Model proposal and deterministic derivation are not occurrence sources. | Closed source validator. | `MODEL_PROPOSAL` and `DETERMINISTIC_DERIVATION` fail `...SOURCE_INVALID`. |
| Source role is not performer role. | A reporter/source does not thereby become performer, executor, assignee, owner, or accountable actor. | No performer field or actor relation exists. | `CLAIM SOURCE ROLE != PERFORMER ROLE`; no actor-ID equality or inequality is required or inferred. |
| Claim does not infer a relation to 8A state. | Prior decision, intent, or commitment must remain independently represented. | No predecessor field or relation resolver. | Text, actor, ID, and temporal similarity establish no relation. |
| Authoritative reference is not authority or support. | Citation of opaque producer-owned state must not become current authority, semantic support, or reality. | Type-only reference surface; no runtime authority dependency. | `SOURCE PROVENANCE != CURRENT SOURCE AUTHORITY != SEMANTIC OCCURRENCE SUPPORT != AUTHORITY OF REALITY`. |
| Reference values remain exact opaque representation. | Producer-owned identity/locator strings are semantic claim state. | Non-blank validation without trimming. | Every reference axis affects `DAOC_`; only human actor and operation text may trim in creation. |
| Operation text remains opaque. | Description must not create command, taxonomy, performer, outcome, or Action Intent relation. | String-only canonical local validation. | No action type, target, performer, parameters, workflow, status, or outcome API exists. |
| Phase 8B represents no temporal claim. | Wall-clock data must not be invented as occurrence support. | Exact artifact excludes temporal fields and clock APIs. | `TIMESTAMP != OCCURRENCE PROOF`; `TEMPORAL ORDER != CAUSATION`. |
| `DAOC_` commits complete represented local claim state. | Deterministic identity must retain source origin and all opaque reference axes without becoming reality identity. | Canonical source tuple and SHA-256 identity. | Object insertion order is non-semantic; represented source axis or operation change alters `DAOC_`. |
| Representation errors follow owning semantic boundary. | Outer capture must not mask invalid source/reference representation. | Boundary-local shallow descriptor capture. | Constructor top/source/reference/operation failures map to their exact four owned errors; stored failures collapse to `...INVALID`. |
| Stored assertion is self-contained and non-repairing. | Stored claims need no external authority operation and must not rewrite state. | `assertActionOccurrenceClaim(...)`. | Only otherwise-valid stale ID yields `...ID_MISMATCH`; `CREATE MAY CANONICALIZE; ASSERT MUST NOT REPAIR.` |
| Hostile state rejects safely and return is detached. | Accessors and hidden or extra state cannot become claim authority. | Descriptor-based local capture and detached return. | Accessors, symbols, hidden fields, extras, and applicable self/cycles reject without getter execution; detached does not mean deep-frozen. |
| Claim remains pre-fact/pre-observation state and non-persistent. | A represented source claim must not become Action fact, observed reality, execution proof, outcome, or authority of record. | Exact closed surface with no repository/persister. | `ACTION OCCURRENCE CLAIM != ACTION OCCURRENCE FACT != OBSERVED REALITY != EXECUTION PROOF`; no persistence authority exists. |

## Phase 8C1 state-change-claim invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- |
| Claim is standalone exact two-field input and five-field artifact. | State-change representation must not become a mandatory continuation of decision, intention, commitment, or occurrence-claim state. | Exact closed `state-change-claim` input/artifact. | No Decision, Intent, Commitment, Action Occurrence Claim, revision, assessment, recommendation, coherence, or optional predecessor field exists. |
| Source union is only `HUMAN_INPUT | AUTHORITATIVE_STATE`. | Model proposal and deterministic derivation are not State Change Claim sources. | Closed source validator. | `MODEL_PROPOSAL` and `DETERMINISTIC_DERIVATION` fail `...SOURCE_INVALID`. |
| Source role is not affected-actor role. | A reporting source does not thereby become affected actor, performer, executor, assignee, owner, or accountable actor. | No affected-actor or performer field exists. | `STATE CHANGE CLAIM SOURCE ROLE != AFFECTED ACTOR ROLE`; no actor-ID equality or inequality is required or inferred. |
| Claim does not infer a relation to the other claim or 8A paths. | Action occurrence, decision, intent, commitment, and state change remain independently represented. | No predecessor field or relation resolver. | Text, actor, ID, and temporal similarity establish no relation; `ENTITY A + ENTITY B != RELATION A-B`. |
| Action Occurrence Claim plus State Change Claim is not outcome. | Coexisting claims cannot become an effect, consequence, or outcome relation. | Exact closed artifact has no outcome or relation field. | `ACTION OCCURRENCE CLAIM + STATE CHANGE CLAIM != OUTCOME`. |
| Authoritative reference is not authority or support. | Citation of opaque producer-owned state must not become current authority, semantic state-change support, or reality. | Type-only reference surface; no runtime authority dependency. | `SOURCE PROVENANCE != CURRENT SOURCE AUTHORITY`; `CURRENT SOURCE AUTHORITY != SEMANTIC STATE CHANGE SUPPORT`; `SEMANTIC STATE CHANGE SUPPORT != AUTHORITY OF REALITY`. |
| Reference values remain exact opaque representation. | Producer-owned identity/locator strings are semantic claim state. | Non-blank validation without trimming. | Every reference axis affects `DSCC_`; only human actor and description may trim in creation. |
| Description remains opaque. | Text must not create structured delta, effect, outcome, consequence, causal relation, or taxonomy. | String-only canonical local validation. | No before/after state, delta, metric, effect, outcome, consequence, status, or causal API exists. |
| Phase 8C1 represents no temporal claim. | A textual claim must not manufacture a change-time representation or causal relation. | Exact artifact excludes temporal fields and clock APIs. | `CLAIM THAT CHANGE OCCURRED != REPRESENTATION OF WHEN CHANGE OCCURRED`; `TIMESTAMP != STATE CHANGE PROOF`; `TEMPORAL ORDER != CAUSATION`. |
| `DSCC_` commits complete represented local claim state. | Deterministic identity must retain source origin and every opaque reference axis without becoming real-world identity. | Canonical source tuple and SHA-256 identity. | Object insertion order is non-semantic; represented source axis or description change alters `DSCC_`. |
| Representation errors follow owning semantic boundary. | Outer capture must not mask invalid source/reference representation. | Boundary-local shallow descriptor capture. | Constructor top/source/reference/description failures map to their exact four owned errors; stored failures collapse to `...INVALID`. |
| Stored assertion is self-contained and non-repairing. | Stored claims need no external authority operation and must not rewrite state. | `assertStateChangeClaim(...)`. | Only otherwise-valid stale ID yields `...ID_MISMATCH`; `CREATE MAY CANONICALIZE WHERE EXPLICITLY DEFINED; ASSERT MUST NOT REPAIR.` |
| Hostile state rejects safely and return is detached. | Accessors and hidden or extra state cannot become claim authority. | Descriptor-based local capture and detached return. | Accessors, symbols, hidden fields, extras, and applicable self/cycles reject without getter execution; detached does not mean deep-frozen. |
| Claim remains pre-fact/pre-observation state and non-persistent. | A represented source claim must not become state-change fact, observed reality, verified change, effect, outcome, consequence, causal claim, or authority of record. | Exact closed surface with no repository/persister. | `STATE CHANGE CLAIM != STATE CHANGE FACT`; `STATE CHANGE CLAIM != OBSERVED REALITY`; `STATE CHANGE CLAIM != VERIFIED CHANGE`; `STATE CHANGE CLAIM != EFFECT`; `STATE CHANGE CLAIM != OUTCOME`; `STATE CHANGE CLAIM != CONSEQUENCE`; `STATE CHANGE CLAIM != CAUSAL CLAIM`; no persistence authority exists. |

## Phase 8C2 action-state-change-association-proposal invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Proposal is exact three-field input and six-field artifact. | Association representation must remain explicit and bounded. | Exact closed `action-state-change-association` input/artifact. | No outcome, effect, consequence, attribution, causal, relation-kind, status, score, time, or persistence field exists. |
| Endpoint roles consume sealed claims without merging them. | Coexisting claims must not become association or fact. | Public sealed assertions for embedded `ActionOccurrenceClaim` and `StateChangeClaim`. | `ACTION OCCURRENCE CLAIM + STATE CHANGE CLAIM != ASSOCIATION`; construction does not repair either claim. |
| Provenance union is only `HUMAN_INPUT | MODEL_PROPOSAL | AUTHORITATIVE_STATE`. | A proposal boundary must not inherit every provenance role from another phase. | Closed provenance validator. | `DETERMINISTIC_DERIVATION` fails `...PROVENANCE_INVALID`; `SAME REPRESENTATION != SAME SEMANTIC ROLE`. |
| Provenance does not become authority or support. | A represented source must not certify the proposal. | No authority dependency, reader, resolver, evaluator, provider call, or persistence operation. | `PROVENANCE != SUPPORT`; `MODEL PROPOSAL != PUBLICATION AUTHORITY`; `REFERENCE PRESENT != CURRENT SOURCE AUTHORITY`; `CURRENT SOURCE AUTHORITY != ASSOCIATION TRUTH`. |
| Authoritative reference values remain exact opaque representation. | Producer-owned identity/locator strings are represented provenance state. | Non-blank validation without trimming. | Every reference axis affects `DASCA_`; only human actor and model proposal reference may trim at creation. |
| No automatic association inference exists. | Similarity and coexistence are not an association contract. | No detector, relation resolver, relation kind, or deterministic derivation source. | `TEXT EQUALITY != ASSOCIATION`; `ACTOR EQUALITY != ASSOCIATION`; `SOURCE EQUALITY != ASSOCIATION`; `TEMPORAL PROXIMITY != ASSOCIATION`; `TEMPORAL ORDER != ASSOCIATION`. |
| Proposal has no outcome or causal semantics. | A represented association proposal must not become explanation, attribution, or effect. | Exact closed artifact/public surface. | `ASSOCIATION PROPOSAL != RELATION TRUTH`; `ASSOCIATION PROPOSAL != OUTCOME`; `ASSOCIATION PROPOSAL != EFFECT`; `ASSOCIATION PROPOSAL != CONSEQUENCE`; `ASSOCIATION != ATTRIBUTION`; `ASSOCIATION != CAUSATION`. |
| Phase 8C2 represents no temporal relation. | Association state must not manufacture time or causation. | Exact artifact excludes temporal fields and clock APIs. | `ASSOCIATION PROPOSAL != TEMPORAL RELATION`; `TEMPORAL ORDER != ASSOCIATION`; `TEMPORAL ORDER != CAUSATION`. |
| `DASCA_` retains ordered endpoint roles and complete canonical provenance. | Deterministic proposal identity must not collapse endpoint roles or provenance. | Ordered SHA-256 identity tuple. | Object insertion order is non-semantic; either endpoint ID or canonical provenance changes `DASCA_`; `DASCA IDENTITY != RELATION TRUTH`; `DASCA IDENTITY != PERSISTENCE AUTHORITY`. |
| Representation errors follow their semantic boundary. | Top-level, endpoint, provenance, and reference failures need deterministic ownership. | Boundary-local shallow descriptor capture plus sealed endpoint assertions. | Constructor failures use exact input/action-claim/state-change-claim/provenance/reference errors; stored failures are `...INVALID` except a valid body with stale outer ID. |
| Stored assertion is self-contained and non-repairing. | Stored association validity must not invoke external authority or repair inputs. | `assertActionStateChangeAssociationProposal(...)`. | A stale nested `DAOC_` or `DSCC_` is outer `...INVALID`; only otherwise-valid stale outer `DASCA_` is `...ID_MISMATCH`. |
| Hostile state rejects safely and return is detached. | Accessors or hidden representation cannot become proposal authority. | Descriptor-based local capture, sealed endpoint assertions, and detached return. | Accessors, symbols, hidden fields, extras, invalid objects, and hostile nested claims reject without getter execution where applicable; detached does not mean deep-frozen. |
| Structural relation and Legacy Career contracts are not reused. | Existing relation/outcome vocabulary is not generic Phase 8C2 authority. | Import/public-surface boundary. | `STRUCTURAL RELATION PROPOSAL != ACTION STATE CHANGE ASSOCIATION PROPOSAL`; no Career outcome, feedback, or learning import exists. |
| Proposal remains non-persistent. | Represented association must not become authority of record or truth. | Exact closed surface with no repository/persister. | `ASSOCIATION PROPOSAL != PERSISTENCE AUTHORITY`; `PERSISTED != TRUE`. |

## Phase 8C3 outcome-attribution-proposal invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Proposal is exact two-field input and five-field artifact. | Outcome-role attribution remains explicit and bounded. | Exact closed `outcome-attribution-proposal` input/artifact. | No duplicated claims, outcome-state, effect, consequence, causal, score, status, time, or persistence field exists. |
| A sealed association predecessor is required. | Phase 8C3 must add proposal state without taking ownership of association or claim representation. | `assertActionStateChangeAssociationProposal(...)`. | `ASSOCIATION PROPOSAL EXISTENCE != OUTCOME ATTRIBUTION PROPOSAL EXISTENCE`; construction does not repair the association or its embedded claims. |
| No automatic association-to-attribution transition exists. | Association state alone cannot create outcome-attribution state. | Explicit constructor input and no detector or deterministic derivation origin. | `ASSOCIATION PROPOSAL != OUTCOME ATTRIBUTION PROPOSAL`; `ACTION OCCURRENCE CLAIM + STATE CHANGE CLAIM + ASSOCIATION PROPOSAL != OUTCOME ATTRIBUTION PROPOSAL`. |
| Provenance is closed and owns its own semantic role. | Attribution provenance must not alias another phase's source or provenance ontology. | Closed provenance validator. | Only `HUMAN_INPUT | MODEL_PROPOSAL | AUTHORITATIVE_STATE`; `DETERMINISTIC_DERIVATION` fails `...PROVENANCE_INVALID`; `SAME REPRESENTATION != SAME SEMANTIC ROLE`. |
| Same or different concrete sources are both admitted. | Source equality must not become a relation or authority rule. | Provenance capture only. | Association and attribution provenance equality or inequality establishes no additional meaning. |
| Provenance does not become authority or support. | Represented source state cannot certify outcome truth. | No reader, resolver, validator, evaluator, provider invocation, or persistence operation. | `PROVENANCE != SUPPORT`; `REFERENCE PRESENT != CURRENT SOURCE AUTHORITY`; `CURRENT SOURCE AUTHORITY != OUTCOME TRUTH`; `MODEL PROPOSAL != PUBLICATION AUTHORITY`; `MODEL PROPOSAL != OUTCOME TRUTH`; `MODEL PROPOSAL != CAUSAL AUTHORITY`. |
| Authoritative reference values remain exact opaque representation. | Producer-owned identity/locator strings remain represented provenance data. | Non-blank validation without trimming. | Exact reference axes affect `DOATP_`; only human actor ID and model proposal reference may trim at creation. |
| Proposal has no outcome-truth, relation-truth, or causal semantics. | An attribution proposal must not become outcome fact, explanation, or causal proof. | Exact closed artifact/public surface. | `OUTCOME ATTRIBUTION PROPOSAL != OUTCOME TRUTH`; `OUTCOME ATTRIBUTION PROPOSAL != RELATION TRUTH`; `OUTCOME ATTRIBUTION PROPOSAL != CAUSAL CLAIM`; `OUTCOME ATTRIBUTION != CAUSATION`. |
| Phase 8C3 represents no temporal relation. | Attribution state must not manufacture time or causation. | Exact artifact excludes temporal fields and clock APIs. | `TEMPORAL ORDER != OUTCOME ATTRIBUTION`; `TEMPORAL ORDER != CAUSATION`. |
| `DOATP_` identity retains complete provenance. | Deterministic proposal identity must remain over its sealed association and full canonical provenance. | SHA-256 identity tuple. | Object insertion order is non-semantic; association ID or canonical provenance changes `DOATP_`; `DOATP IDENTITY != OUTCOME TRUTH`; `DOATP IDENTITY != RELATION TRUTH`; `DOATP IDENTITY != CAUSAL IDENTITY`; `DOATP IDENTITY != PERSISTENCE AUTHORITY`. |
| Representation errors follow their semantic boundary. | Top-level, association, provenance, and reference failures need deterministic ownership. | Boundary-local shallow descriptor capture plus sealed predecessor assertion. | Constructor failures use exact input/association-proposal/provenance/reference errors; stored failures are `...INVALID` except a valid body with stale outer ID. |
| Stored assertion is self-contained and non-repairing. | Stored attribution validity must not invoke external authority or rewrite state. | `assertOutcomeAttributionProposal(...)`. | Invalid nested association or embedded claim state is outer `...INVALID`; only otherwise-valid stale outer `DOATP_` is `...ID_MISMATCH`. |
| Hostile state rejects safely and return is detached. | Hostile representation cannot become attribution authority. | Descriptor-based local capture, sealed association assertion, and detached return. | Accessors, symbols, hidden fields, extras, invalid objects, and hostile predecessor state reject without getter execution where applicable; detached does not mean deep-frozen. |
| Proposal remains non-persistent and does not reuse Legacy Career. | Represented attribution proposal must not become authority of record, outcome truth, or legacy ontology. | Exact closed surface and import/public-surface boundary. | `OUTCOME ATTRIBUTION PROPOSAL != PERSISTENCE AUTHORITY`; `PERSISTED != TRUE`; no Career outcome, feedback, or learning import exists. |

## Phase 8D1 decision-context-observation-proposal invariants

| Rule | Why | Enforcement location | Failure mode / contract |
| --- | --- | --- | --- |
| Proposal is exact three-field input and six-field artifact. | An observation-role candidate must remain explicit and bounded before any Decision Context operation. | Exact closed `context-observation-proposal` input/artifact. | No `role`, Context Item, Context, revision, feedback, evaluation, support, truth, time, repository, or persistence field exists. |
| A sealed Outcome Attribution Proposal predecessor is required. | Phase 8D1 must add candidate state without taking ownership of its preceding association or claims. | `assertOutcomeAttributionProposal(...)`. | `OUTCOME ATTRIBUTION PROPOSAL EXISTENCE != DECISION CONTEXT OBSERVATION PROPOSAL EXISTENCE`; construction does not repair or reinterpret the predecessor. |
| No automatic return-path transition exists. | A predecessor alone cannot produce an observation candidate. | Explicit input requires predecessor, statement, and provenance; no detector exists. | `OUTCOME ATTRIBUTION PROPOSAL != DECISION CONTEXT OBSERVATION PROPOSAL`; `REENTRY PROPOSAL != LOOP CLOSED`. |
| Statement is explicit, opaque, canonical, and identity-bearing. | The candidate statement must not be inferred from previously represented text or IDs. | Trimmed nonempty statement capture and DCOP identity tuple. | `OUTCOME ATTRIBUTION PROPOSAL != OBSERVATION STATEMENT`; stored noncanonical statement is `...INVALID`. |
| Provenance is closed and owns its own semantic role. | Observation-candidate provenance must not alias context-item or predecessor provenance semantics. | Closed provenance validator. | Only `HUMAN_INPUT | MODEL_PROPOSAL | AUTHORITATIVE_STATE`; `DETERMINISTIC_DERIVATION` fails `...PROVENANCE_INVALID`; `SAME REPRESENTATION != SAME SEMANTIC ROLE`. |
| Provenance does not become authority or support. | Represented source state cannot certify observation or outcome truth. | No reader, resolver, payload inspection, validator, evaluator, model invocation, repository, or persistence operation. | `PROVENANCE != SUPPORT`; `REFERENCE PRESENT != CURRENT SOURCE AUTHORITY`; `CURRENT SOURCE AUTHORITY != OBSERVATION TRUTH`; `MODEL PROPOSAL != PUBLICATION AUTHORITY`; `MODEL PROPOSAL != OBSERVATION TRUTH`; `MODEL PROPOSAL != OUTCOME TRUTH`. |
| No Context Item, admission, revision, or loop closure exists. | A candidate for future Decision Context consideration must remain pre-admission state. | Exact closed surface has no context/revision operation. | `DECISION CONTEXT OBSERVATION PROPOSAL != DECISION CONTEXT ITEM`; `DECISION CONTEXT OBSERVATION PROPOSAL != DECISION CONTEXT`; `DECISION CONTEXT OBSERVATION PROPOSAL != DECISION CONTEXT REVISION`; `REENTRY PROPOSAL != ADMISSION`; `REENTRY PROPOSAL != REVISION`; `REENTRY PROPOSAL != LOOP CLOSED`. |
| Observation candidate does not promote truth, support, or causation. | An OBSERVATION role is not observed reality or semantic validation. | Exact closed artifact/public surface. | `OBSERVATION ROLE != OBSERVED REALITY`; `OBSERVATION PROPOSAL != OBSERVATION TRUTH`; `REENTRY != OUTCOME TRUTH`; `REENTRY != SEMANTIC SUPPORT`; no causation or Feedback/Learning semantics exist. |
| `DCOP_` identity retains predecessor, statement, and complete provenance. | Deterministic candidate identity must not become admission or revision identity. | SHA-256 identity tuple. | `DCOP IDENTITY != OBSERVATION TRUTH`; `DCOP IDENTITY != CONTEXT ADMISSION`; `DCOP IDENTITY != REVISION IDENTITY`; `DCOP IDENTITY != OUTCOME TRUTH`; `DCOP IDENTITY != PERSISTENCE AUTHORITY`. |
| Representation errors follow their semantic boundary. | Top-level, predecessor, statement, provenance, and reference failures need deterministic ownership. | Boundary-local shallow descriptor capture plus sealed predecessor assertion. | Constructor failures use exact input/outcome-attribution/statement/provenance/reference errors; stored failures are `...INVALID` except a valid body with stale outer ID. |
| Stored assertion is self-contained and non-repairing. | Stored candidate validity must not invoke external authority or rewrite state. | `assertDecisionContextObservationProposal(...)`. | Invalid nested Outcome Attribution, association, Action Occurrence Claim, or State Change Claim state is outer `...INVALID`; only otherwise-valid stale outer `DCOP_` is `...ID_MISMATCH`. |
| Hostile state rejects safely and return is detached. | Hostile predecessor or local representation cannot become candidate authority. | Descriptor-based local capture, sealed predecessor assertion, and detached return. | Accessors, symbols, hidden fields, extras, hostile nested association, Action Occurrence Claim, and State Change Claim state reject without getter execution; detached does not mean deep-frozen. |
| Proposal remains non-persistent and independent of Legacy Career. | Return-path representation must not become authority of record or legacy feedback/learning ontology. | Exact closed surface and import/public-surface boundary. | `DCOP IDENTITY != PERSISTENCE AUTHORITY`; `PERSISTED != TRUE`; no Career outcome, feedback, or learning import exists. |

## Phase 8D2 decision-context-observation-admission invariants

| Invariant | Why it is required | Enforcement boundary | Consequence / excluded inference |
| --- | --- | --- | --- |
| The artifact has exactly three input fields and six stored fields. | A positive admission declaration must remain a narrow human normative boundary. | Exact top-level descriptor capture and exact stored assertion. | Only `decisionContextObservationProposal`, `admittedBy`, and `rationale` enter construction; no target Context, item, revision, status, score, confidence, or time field exists. |
| A sealed DCOP predecessor and explicit construction are required. | A candidate proposal cannot automatically become admitted declaration state. | `assertDecisionContextObservationProposal(...)` at the sealed predecessor boundary. | `DECISION CONTEXT OBSERVATION PROPOSAL != DECISION CONTEXT OBSERVATION ADMISSION DECLARATION`; `PROPOSAL EXISTENCE != ADMISSION DECLARATION EXISTENCE`; no automatic transition exists. |
| `admittedBy` is exactly human and admission is positive only. | Phase 8D2 records a human declaration, not a source/provenance union, verdict, or voting system. | Exact actor capture: `{ origin: "HUMAN_INPUT", actorId }`. | No reject, defer, ignore, abstain, block, aggregation, consensus, ranking, priority, score, confidence, or decision status exists; absence does not infer any negative disposition. |
| Admission actor is independent of proposal provenance. | Declared human admission must not be silently authorized by predecessor source representation. | Canonical local actor validation without equality comparison. | `ADMITTED BY != PROPOSAL PROVENANCE`; `PROPOSAL PROVENANCE != ADMISSION AUTHORITY`; `HUMAN ADMISSION DECLARATION != AUTHENTICATED IDENTITY`; `HUMAN ADMISSION DECLARATION != EXTERNAL AUTHORIZATION`. |
| Rationale remains opaque. | A local human rationale cannot become evidence or semantic validation. | Trimmed nonempty string or exact `null`; no parser or evaluator. | `RATIONALE != EVIDENCE`; `RATIONALE != SUPPORT`; `RATIONALE != OBSERVATION TRUTH`; `RATIONALE != OUTCOME TRUTH`. |
| No target Context or source inventory is represented. | Eligibility must not be confused with a destination or Context mutation. | Closed artifact surface and no Context imports/operations. | `ADMISSION AUTHORITY != MATERIALIZATION TARGET`; `ADMISSION DECLARATION != MATERIALIZATION`; `ADMISSION DECLARATION != CONTEXT MUTATION`; `ADMISSION DECLARATION != REVISION CREATION`; `ADMISSION DECLARATION != LOOP CLOSED`. |
| A sealed authoritative DCOP reference is not promoted or inventoried. | Admission must not alter or admit producer state references into a future Context. | Sealed DCOP assertion only; no resolver, reader, payload, or Context operation. | `AUTHORITATIVE DCOP ADMISSION != SOURCE STATE REFERENCE ADMISSION`; `REFERENCE PRESENT IN DCOP != REFERENCE PRESENT IN FUTURE DECISION CONTEXT`; `ADMISSION DECLARATION != SOURCE STATE INVENTORY MUTATION`. |
| The complete DCOP identity is retained rather than compressed into a possible future item identity. | Distinct return-path candidates must remain representable even if later projection could collide. | Complete sealed predecessor capture and DCOAD identity tuple. | `DCOP IDENTITY != DECISION CONTEXT ITEM IDENTITY`; `DISTINCT DCOP IDENTITY != NECESSARILY DISTINCT FUTURE DCI IDENTITY`; no future mapping is defined here. |
| `DCOAD_` identity is deterministic and complete. | Declaration identity must bind the represented predecessor, declared actor, and rationale exactly. | SHA-256 tuple over sealed DCOP ID, `['HUMAN_INPUT', actorId]`, and rationale including `null`. | `DCOAD IDENTITY != CONTEXT ITEM IDENTITY`; `DCOAD IDENTITY != CONTEXT IDENTITY`; `DCOAD IDENTITY != REVISION IDENTITY`; `DCOAD IDENTITY != OBSERVATION TRUTH`; `DCOAD IDENTITY != OUTCOME TRUTH`; `DCOAD IDENTITY != PERSISTENCE AUTHORITY`. |
| No truth, support, causation, Feedback, or Learning is promoted. | Human admission eligibility is not epistemic or causal proof. | Exact closed artifact/public surface. | `ADMISSION != OBSERVATION TRUTH`; `ADMISSION != OBSERVED REALITY`; `ADMISSION != OUTCOME TRUTH`; `ADMISSION != SEMANTIC SUPPORT`; `ADMISSION != CAUSATION`; no Feedback or Learning semantics exist. |
| Representation safety preserves semantic error ownership. | Hostile outer, actor, or sealed predecessor state cannot be used to bypass admission boundaries. | Boundary-local shallow descriptor capture plus sealed predecessor assertion. | Accessors, symbols, hidden fields, extras, and hostile nested predecessor state reject without getter execution; constructor ownership is input/proposal/actor/rationale and stored failure is outer `...INVALID`. |
| Stored assertion is non-repairing and return is detached. | Stored declaration validity must not rewrite human or predecessor state. | Exact canonical assertion and detached construction result. | `ASSERT MUST NOT REPAIR`; only an otherwise-valid stale outer `DCOAD_` is `...ID_MISMATCH`; detached does not mean deep-frozen. |
| No persistence or Legacy Career reuse exists. | The declaration cannot become authority of record or inherit feedback/learning ontology. | No repository/persister/database operation and no legacy imports. | `PERSISTED != TRUE`; `DCOAD IDENTITY != PERSISTENCE AUTHORITY`; Legacy Career outcome, feedback, and learning semantics remain non-authoritative. |

## Phase 8D3 decision-context-observation-item-projection invariants

| Invariant | Why it is required | Enforcement boundary | Consequence / excluded inference |
| --- | --- | --- |
| One sealed DCOAD is required and projection is explicit. | Admission declaration existence cannot silently become projection state. | Exact one-field input plus `assertDecisionContextObservationAdmissionDeclaration(...)`. | `DECISION CONTEXT OBSERVATION ADMISSION DECLARATION != DECISION CONTEXT OBSERVATION ITEM PROJECTION`; `ADMISSION != PROJECTION`; no automatic transition exists. |
| Projection derives the exact fixed OBSERVATION input semantics. | Future item semantics must remain structurally determined by the sealed admission. | Deterministic projection from sealed DCOAD. | Role is `OBSERVATION`; statement and provenance exactly preserve the sealed DCOP values. |
| Semantic provenance is preserved, not replaced by operation or admission provenance. | Deterministic transformation must not change the represented observation source. | Exact projected-provenance capture and comparison with sealed DCOP provenance. | `PROJECTED ITEM PROVENANCE = DCOP PROVENANCE`; `ADMISSION AUTHORITY != PROJECTED ITEM PROVENANCE`; `DETERMINISTIC PROJECTION != DETERMINISTIC_DERIVATION ITEM PROVENANCE`; `ADMITTED BY != PROJECTED ITEM PROVENANCE`. |
| Complete admission lineage remains embedded. | Equal projected payloads must not collapse distinct human admission declarations. | Artifact retains complete sealed DCOAD and DCOIP hashes DCOAD identity. | `DISTINCT ADMISSION IDENTITY != NECESSARILY DISTINCT PROJECTED ITEM INPUT`. |
| No item materialization, identity, Context target, or membership exists. | A context-independent semantic projection must stop before Context-relative validation. | Exact five-field artifact and no Context construction/validation operation. | `PROJECTION != DECISION CONTEXT ITEM`; `PROJECTED ITEM INPUT != DECISION CONTEXT ITEM`; `PROJECTED ITEM INPUT != ITEM MEMBERSHIP`; `ITEM IDENTITY COMPUTABILITY != ITEM EXISTENCE`; `ITEM EXISTENCE != CONTEXT MEMBERSHIP`; `PROJECTION != MATERIALIZATION`; `PROJECTION != TARGET CONTEXT`. |
| Authoritative reference carriage does not promote inventory membership. | A preserved producer reference cannot become future Context source admission. | Exact copied sealed provenance; no reader, resolver, payload, repository, or inventory operation. | `REFERENCE CARRIED BY PROJECTED ITEM INPUT != SOURCE STATE INVENTORY MEMBERSHIP`; `PROJECTED AUTHORITATIVE ITEM INPUT != SOURCE STATE REFERENCE ADMISSION`; `REFERENCE PRESENT IN PROJECTED ITEM INPUT != REFERENCE PRESENT IN FUTURE DECISION CONTEXT`. |
| `DCOIP_` identity is deterministic only over the sealed admission identity. | Projection identity retains distinct admission lineage without claiming item or Context identity. | SHA-256 tuple over schema and DCOAD ID. | `DCOIP IDENTITY != DCI IDENTITY`; `DCOIP IDENTITY != CONTEXT IDENTITY`; `DCOIP IDENTITY != REVISION IDENTITY`; `DCOIP IDENTITY != OBSERVATION TRUTH`; `DCOIP IDENTITY != PERSISTENCE AUTHORITY`. |
| Projection promotes no truth, support, causation, Feedback, or Learning. | Structural consistency with a sealed predecessor is not semantic validation. | Exact closed projection surface. | `PROJECTION != OBSERVATION TRUTH`; `PROJECTION != OBSERVED REALITY`; `PROJECTION != OUTCOME TRUTH`; `PROJECTION != SEMANTIC SUPPORT`; `PROJECTION != CAUSATION`; no Feedback or Learning semantics exist. |
| Representation safety preserves boundary ownership. | Hostile projection, projected input/provenance, reference, or nested predecessor state cannot bypass assertion. | Boundary-local shallow descriptor capture plus sealed predecessor assertion. | Extras, hidden fields, symbols, and accessors across all relevant boundaries reject without getter execution; stored body invalidity is `...INVALID`. |
| Stored assertion is non-repairing and return is detached. | Projection validity must not rewrite the sealed lineage or projected semantics. | Exact stored assertion and detached construction result. | Only an otherwise-valid stale outer `DCOIP_` is `...ID_MISMATCH`; body drift takes precedence; detached does not mean deep-frozen. |
| No time, persistence, Context mutation, revision, or Legacy reuse exists. | Projection must not become an authority-of-record or lifecycle transition. | Exact closed surface and import boundary. | `PROJECTION != CONTEXT MUTATION`; `PROJECTION != DECISION CONTEXT REVISION`; `PROJECTION != REVISION CREATION`; `PROJECTION != LOOP CLOSED`; `PERSISTED != TRUE`; Legacy Career remains non-authoritative. |

## Phase 8D4A DecisionContextObservationTargetDeclaration invariants

| Invariant | Why it is required | Enforcement boundary | Consequence / excluded inference |
| --- | --- | --- | --- |
| One sealed DCOIP and an explicit target declaration are required. | Projection existence must not silently name a base state or create target-declaration state. | Exact four-field input plus `assertDecisionContextObservationItemProjection(...)`. | `DCOIP EXISTENCE != TARGET DECLARATION EXISTENCE`; `ONE DCOIP != ONE TARGET DECLARATION`; no automatic transition exists. |
| The declared revision reference is DREV shape only. | A syntactically shaped reference cannot become an exact bound revision or authority fact. | `^DREV_[0-9A-F]{24}$` validation only; no reader, repository, or revision assertion. | `DREV SHAPE != REVISION EXISTENCE`; `TARGET REVISION ID != SEALED REVISION`; `TARGET REVISION ID != PERSISTENCE PROOF`; `TARGET REVISION ID != CURRENT REVISION`; `TARGET REVISION ID != HEAD REVISION`; `TARGET REVISION ID != LATEST REVISION`. |
| The reference remains a base state, not a mutation destination. | Naming prior state cannot be an insertion or mutation operation. | Closed declaration surface with no Context or revision object. | `TARGET REVISION != MUTATION DESTINATION`; `TARGET REVISION ID != FUTURE REVISION ID`; `TARGET DECLARATION != REVISION MUTATION`; `TARGET DECLARATION != REVISION CREATION`. |
| Declarer is human-only and declarative. | A represented actor must not gain predecessor, identity, authorization, ownership, or authorship semantics. | Exact `HUMAN_INPUT` actor capture and canonical trimmed assertion. | `DECLARED BY != ADMITTED BY`; `DECLARED BY != PROJECTION PROVENANCE`; `DECLARED BY != AUTHENTICATED IDENTITY`; `DECLARED BY != EXTERNAL AUTHORIZATION`; `DECLARED BY != REVISION OWNER`; `DECLARED BY != REVISION AUTHOR`. |
| Rationale remains opaque and identity-bearing. | Human text cannot validate a target or become support. | Exact `string \| null` local canonicalization only. | `RATIONALE != EVIDENCE`; `RATIONALE != SUPPORT`; `RATIONALE != TARGET VALIDITY`; `RATIONALE != REVISION EXISTENCE`; `RATIONALE != MATERIALIZATION AUTHORITY`. |
| `DCOTD_` identity commits to complete declared state. | Declaration identity must preserve projection, reference, actor, and rationale axes without claiming a bound revision. | SHA-256 tuple over schema, DCOIP ID, target ID, declared actor, and rationale. | `DCOTD IDENTITY != REVISION EXISTENCE`; `DCOTD IDENTITY != TARGET BINDING`; `DCOTD IDENTITY != MATERIALIZATION`; `DCOTD IDENTITY != PERSISTENCE AUTHORITY`; `DCOTD IDENTITY != OBSERVATION TRUTH`; `DECLARATION IDENTITY != PERSISTENCE AUTHORITY`. |
| Target declaration stops before binding and materialization. | A base-state declaration cannot create readiness, a Context Item, membership, Context mutation, a revision, or loop closure. | No reader, Context constructor, validation, or revision operation. | `TARGET DECLARATION != TARGET BINDING`; `TARGET DECLARATION != MATERIALIZATION`; `TARGET DECLARATION != MATERIALIZATION READINESS`; `TARGET DECLARATION != DECISION CONTEXT ITEM`; `TARGET DECLARATION != ITEM MEMBERSHIP`; `TARGET DECLARATION != CONTEXT MEMBERSHIP`; `TARGET DECLARATION != CONTEXT MUTATION`; `TARGET DECLARATION != LOOP CLOSED`. |
| Carried authoritative provenance does not become inventory admission. | An embedded reference cannot silently become a target Context source-state fact. | Sealed DCOIP assertion only; no target inspection, resolver, payload, or inventory operation. | `TARGET DECLARATION != SOURCE STATE REFERENCE ADMISSION`; `TARGET DECLARATION != SOURCE STATE INVENTORY MEMBERSHIP`; `TARGET REVISION REFERENCE != MATERIALIZATION READINESS`; `AUTHORITATIVE REFERENCE CARRIED BY DCOIP != REFERENCE PRESENT IN TARGET REVISION CONTEXT`. |
| No truth, support, causation, Feedback, Learning, time, persistence, or Legacy reuse exists. | Declaration is represented governance state only. | Exact closed public surface and no external operation/import. | `TARGET DECLARATION != OBSERVATION TRUTH`; `TARGET DECLARATION != OBSERVED REALITY`; `TARGET DECLARATION != OUTCOME TRUTH`; `TARGET DECLARATION != SEMANTIC SUPPORT`; `TARGET DECLARATION != CAUSATION`; `PERSISTED != TRUE`; Legacy Career remains non-authoritative. |
| Representation safety and stored assertion are non-repairing. | Hostile state cannot bypass declaration ownership or rewrite canonical representation. | Boundary-local shallow descriptor capture plus sealed DCOIP assertion. | Extras, hidden fields, symbols, and accessors reject without getter execution; stored body invalidity is `...INVALID`; only otherwise-valid stale `DCOTD_` is `...ID_MISMATCH`; returned state is detached, not deep-frozen. |
