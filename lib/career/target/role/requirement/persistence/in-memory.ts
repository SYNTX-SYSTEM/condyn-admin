import type { TargetRequirementRevision } from "../types";
import { createBoundTargetRequirementRevisionPersister, sameTargetRequirementRevisionData } from "./persister";
import type { BoundTargetRequirementRevisionPersister, TargetRequirementLineageLookup, TargetRequirementRevisionRepository } from "./types";
export class InMemoryTargetRequirementRevisionRepository implements TargetRequirementRevisionRepository { readonly #values = new Map<string, TargetRequirementRevision>(); constructor(private readonly lineage: TargetRequirementLineageLookup) {} async getRevisionById(id: string): Promise<TargetRequirementRevision | null> { const value = this.#values.get(id); return value === undefined ? null : structuredClone(value); } createTargetRequirementRevisionPersister(): BoundTargetRequirementRevisionPersister { return createBoundTargetRequirementRevisionPersister({ getRevisionById: this.getRevisionById.bind(this), getTargetRoleProfileRevisionById: this.lineage.getTargetRoleProfileRevisionById.bind(this.lineage), getTargetRoleOrganizationBindingRevisionById: this.lineage.getTargetRoleOrganizationBindingRevisionById.bind(this.lineage), getTargetOrganizationRevisionById: this.lineage.getTargetOrganizationRevisionById.bind(this.lineage), writeRevision: this.#write.bind(this) }); } async #write(value: TargetRequirementRevision): Promise<void> { const prior = this.#values.get(value.targetRequirementRevisionId); if (!prior) { this.#values.set(value.targetRequirementRevisionId, structuredClone(value)); return; } if (!sameTargetRequirementRevisionData(prior, value)) throw new Error("ERR_TARGET_REQUIREMENT_REVISION_IMMUTABLE_CONFLICT"); } }
